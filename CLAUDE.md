# Target — Offline Exam Prep Tracker

## What this app is

A personal, fully offline Android app for organizing exam study materials and tracking study progress. Not tied to any specific exam — the user defines whatever exam(s) they're preparing for, then organizes materials underneath. It's now genuinely multi-account (see below) — "personal" describes the deployment (one phone, not distributed), not a single-user data model.

**The data model was fully reset once already** (see git history around "Full reset" for what came before — nested Topics, Question Banks, Practice Exams, Lecture Notes, Test Attempts, pace estimation). None of that survived the reset; don't resurrect ideas from it without the user asking again. What's described below is the current, second-generation model, now with accounts layered on top.

### Accounts

Real multi-account auth, not a single-profile gate — `Register` can create additional accounts at any time; `Login` looks up by username. `User`: firstName, middleName (optional), lastName, username (unique), email (optional), passwordHash/passwordSalt (`data/PasswordHasher.kt` — salted SHA-256 via `SecureRandom`/`MessageDigest`, no new dependency; passwords are never stored or compared in plaintext), preferredName + preferredNameSource.

**Preferred name** is chosen via a toggle (First / Middle) plus three mutually-exclusive checkboxes (Last Name / Username / Other — "Other" reveals a free-text field), not typed directly — `ui/common/PreferredNameSelector.kt`, shared between Register and Edit Profile. The choice **stays live**: `User.preferredNameSource` is stored, and `data/PreferredNameResolver.kt`'s `resolvePreferredName()` recomputes the stored `preferredName` string from the current source field every time the profile is saved — so editing First Name later automatically updates Preferred Name too, if that's the active source. This is the one place in the app where a value is deliberately kept in sync via recomputation-on-save rather than either a live read-time join or a frozen one-time value.

Session persistence and the light/dark theme preference both live in `data/local/AppPreferences.kt` — a plain `SharedPreferences` wrapper (deliberately not DataStore, no new dependency needed for two small values). `currentUserId` is still read once per ViewModel at construction, not observed reactively — Log Out and Switch Account (see Navigation shell below) both clear/replace it and then force a full back-stack reset (`popUpTo(0)` to `LoginRoute`/`HomeRoute`) rather than relying on any ViewModel noticing the change mid-process, so the "read once" assumption still holds; there just wasn't a codepath that changed it before. `isDarkMode` **is** a `StateFlow`, since `MainActivity` needs to react immediately when Settings toggles it.

`Exam` and `Course` carry a nullable `userId` (FK, cascade) — nullable because real pre-auth exam data already existed on-device with no owner when accounts were added; those rows just don't appear once list queries filter by the logged-in user, rather than being destroyed. Every exam/course query and insert must be scoped by `AppPreferences.currentUserId` — there is no global, cross-account exam or course data (unlike `ExamType`, which stays a shared/seeded lookup table, not per-user).

### Exam data (unchanged in shape since the last update, now user-scoped)

Hierarchy: **Exam → (optional) Section → Courses**, plus a per-user **Course** pool, a per-user **Language** pool, and a shared, seeded **ExamType** list.

- **ExamType**: a real, seeded table (not an enum) — currently "University Entrance Exam", "High School Entrance Exam", "Language Exam", "Vocational Exam". Seeded idempotently on every app start (`ExamTypeDao.seedDefaultsIfEmpty()`, called from `TargetApplication.onCreate()`) rather than via `RoomDatabase.Callback.onCreate()` — that callback does **not** reliably fire when the schema is created via a destructive-fallback recreation rather than a genuine first-run (confirmed on-device). Managing this list (add/delete/rename by the user) is an explicitly deferred next step — don't build that screen unless asked. Shared across all accounts, not per-user. `ExamTypeDao.kt` exports `LANGUAGE_EXAM_TYPE_NAME` (= "Language Exam") as the one place that magic string lives — everywhere else that needs to know "is this exam a language exam" compares against that constant rather than a re-typed literal.
- **Exam**: name, `examTypeId` (FK), `userId` (FK, nullable — see Accounts), `hasSections: Boolean` (decided at creation, not editable afterward — toggling it later raises unresolved questions about what happens to existing sections/courses), `examDate` (meaningful only when `!hasSections`), optional `studyStartDate`, optional `languageId` (FK to Language, `SET_NULL` on delete — meaningful only when the exam's type is Language Exam; see Language below), optional `level: String?` (schema v16 — meaningful only for a Language Exam, same scoping as `languageId`; see Language below for the full split).
- **Language**: per-user, reusable, free-text name (e.g. "German") — same shape as `Course` minus icons (no icon picker for languages, unlike courses). Has its own full CRUD screen (`ui/languagelist/`, reached from Home's drawer, right after Courses) mirroring `ui/courselist/`'s list/add/edit/delete pattern. `AddOrEditExamDialog` (`ui/common/ExamFormDialog.kt`) shows a Language combobox *only* when the selected exam type's name equals `LANGUAGE_EXAM_TYPE_NAME` — it's not shown for any other exam type, and the dialog nulls out the selection on save if the type gets changed away from Language Exam. The chosen language isn't just stored silently: it also surfaces in `ExamListScreen`'s row subtitle (`examType · language · date`) and `ExamDetailScreen`'s summary (`Language: X`), the same way `examDate` already does, so picking one has visible confirmation after saving. Because deleting a Language `SET_NULL`s any exam's `languageId` that pointed to it, a Language Exam can end up with `languageId == null` two ways (never set, or set-then-deleted) — both render as an explicit "No language set" in the row/summary rather than silently omitting the line, so a Language Exam always shows *some* language state. Non-Language-Exam types never show the line at all, set or not.

**Publisher / Level split**: by explicit request ("German A2 certificate... you take the exam from Goethe. its exam's publisher but your level is A2. lets separate it"), a Language Exam's certifying body and proficiency level — previously crammed into one free-text `Exam.name` (e.g. "Goethe A2" as a single string) — are now two separate concepts: `Exam.name` holds just the publisher ("Goethe"), and the new `Exam.level: String?` (schema v16, plain nullable `ADD COLUMN`, same no-backfill shape as every other added column with no inferable source) holds the level. `level` is **free text, not a fixed CEFR dropdown** — confirmed via clarifying question, since proficiency scales differ by language family (CEFR A1–C2 for German/French/Spanish/Italian, but JLPT N5–N1 for Japanese, HSK 1–6 for Chinese, TOPIK for Korean, etc.) and a fixed European-only dropdown would lock out every non-CEFR language; free text matches how Course/Topic/Study Resource names already work everywhere else in the app. `AddOrEditExamDialog`'s Name field relabels to "Publisher" (`exam_field_publisher`) whenever the selected type is Language Exam (English default: "Exam name" otherwise), and a new optional "Level" text field (`exam_field_level`) appears directly under the Language combobox, only for that same type — `level` is nulled out on save exactly like `languageId` whenever the type isn't Language Exam. Existing real data (e.g. the on-device "Goethe A2" exam, created before this split) isn't retroactively parsed apart — the new column comes back `null` and the exam keeps reading as "Goethe A2" via `name` alone until manually re-split through the edit dialog, the same "orphaned rather than destroyed" precedent as every other schema addition with no reliable source to infer from.

`ui/common/ExamDisplayLabel.kt`'s `examDisplayLabel(exam)` is a small plain (non-`@Composable`, no string resources needed) helper — `"$name $level"` when `level` is set, else just `name` — used everywhere the exam's short label already appeared before this split: `ExamListScreen`'s row headline and `ExamDetailScreen`'s top-bar title. `ExamDetailScreen`'s summary also gained a "Level: X" line (`examdetail_level`) directly under "Language: X", following the exact same "always show *some* state, `common_not_set` fallback, never silently omit the line" convention already established for language.

By the same follow-up request ("show what level exam is that and which language's exam as well like 'Goethe - A2 - German'"), Home's Upcoming card uses a **different, dash-joined, language-inclusive** format instead of `examDisplayLabel` — `HomeViewModel`'s private `homeExamLabel(item: ExamWithType)` builds `listOfNotNull(name, level, languageName).joinToString(" - ")` for a Language Exam (any missing part is simply skipped, not shown as an empty segment), falling back to the plain `name` for every other exam type. This is deliberately scoped to Home only, not applied to `examDisplayLabel` itself, since `ExamWithType` (unlike the plain `Exam` entity) already carries `languageName` from its join, and `ExamListScreen`/`ExamDetailScreen` already surface the language on a separate line of their own — repeating it inline in their title would be redundant there in a way it isn't on Home's single-line Upcoming row. Confirmed on-device against the real Goethe A2 exam (migrated cleanly from v15, `level` read back `null`): the Edit dialog's Name field showed as "Publisher" with "Goethe A2" in it; splitting it to Publisher "Goethe" + Level "A2" and saving correctly updated the summary to show "Level: A2", the screen title back to "Goethe A2" (recombined via `examDisplayLabel`), the Exams list row to "Goethe A2" (subtitle already showing "Language Exam · German · Nov 30, 2026"), and Home's Upcoming card to exactly **"Goethe - A2 - German"**.
- **Course**: per-user, reusable, free-text name (e.g. "Chemistry"), plus an optional `icon` (nullable `String` key, e.g. `"functions"`, `"science"`), a nullable `examTypeId` (FK to `ExamType`, `SET_NULL` on delete) — a course belongs to exactly one exam type (e.g. "Math" only ever lives under University Entrance Exam), fixed at creation but **reassignable later from `CourseDetailScreen`'s edit dialog** (see below) — unlike `Exam.hasSections`, this one deliberately isn't a "decided once" field — and a nullable `category: CourseCategory?` (`QUANTITATIVE`/`VERBAL`, `data/local/entity/CourseCategory.kt`), for University Entrance/High School Entrance/Vocational courses only (Language Exam never has `Course` rows at all — see below). The exam-level "Add course" dialog (`ExamDetailScreen`) shows the user's not-yet-attached courses as a **multi-select checklist** (`ExamDetailViewModel.availableCoursesToAdd`, checked ones added together via `addExistingCourses`) alongside a text field for creating a new course inline (create-if-missing, case-insensitive name match) — both are now scoped to the exam's own `examTypeId`, not the user's whole course pool, so a Vocational exam can never pick up a University-tagged course. It used to be blind free-text entry only — an already-created course had no visible way to be picked, only re-typed exactly — fixed after being reported as a bug. A course created inline this way (or via the section picker) gets no icon *and no category* at creation — both are only assignable from `CourseDetailScreen`'s edit dialog, not speculatively exposed in every place a course can be created. The category field is a 3-segment toggle (Unset / Quantitative / Verbal) — originally its own private `CourseCategoryField`/`CategoryToggleSegment` copy in `CourseDetailScreen.kt` (the third deliberate reuse of the `PreferredNameSelector` segmented-toggle visual shape, matching how the app had handled that pattern twice before), now backed by the shared `ui/common/SegmentedToggle.kt` — see Interface redesign below. `CourseListByTypeScreen`, `ExamDetailScreen`'s Courses pool, and `SectionDetailScreen`'s assigned-courses list all group courses into **Quantitative / Verbal / Uncategorized** sections (in that order, a group only rendered when non-empty) instead of one flat list — added after the flat list was reported as looking "crowded," then extended to sections by explicit request (initially scoped out — see `SectionCourseWithCourse` note below). Each group has its own collapsible header (chevron, default closed, same `KeyboardArrowDown`/`KeyboardArrowUp` pattern as the Courses/Sections sections below) — a `*GroupHeader` composable, one private copy per screen (`courseGroups()`/`CourseGroupHeader` in `CourseListByTypeScreen.kt`, `examCourseGroups()`/`CourseGroupHeader` in `ExamDetailScreen.kt`, `sectionCourseGroups()`/`SectionCourseGroupHeader` in `SectionDetailScreen.kt`), each over its own screen's `Course.category` / join projection's `courseCategory` field. `SectionCourseDao`'s join query and `SectionCourseWithCourse` gained `courseCategory` the same way `ExamCourseWithCourse` did, once the grouping was extended there too.
- **Courses screen, restructured by exam type**: `CourseListScreen` (`ui/courselist/`, still the drawer's "Courses" destination) is no longer a flat CRUD list — it's a menu of one row per `ExamType` (`"${examType.name} Courses"`), read straight from the seeded `ExamType` table so the order always matches (University Entrance, High School Entrance, Language, Vocational). Tapping a non-Language-Exam row drills into `CourseListByTypeScreen`/`CourseListByTypeViewModel` (`CourseListByTypeRoute(examTypeId)`) — a back-arrow `Scaffold` (not `AppShell`, matching the `ExamDetailScreen`/`SectionDetailScreen` drill-down exception) listing courses (name+icon only, no inline actions) with an "Add course" FAB (name+icon, `examTypeId` fixed to that bucket, via `CourseDao.getByUserIdAndExamTypeId`). Tapping a course row navigates to `CourseDetailScreen` (see below), not straight to Study Resources.
- **CourseDetailScreen / LanguageDetailScreen** (`ui/coursedetail/`, `ui/languagedetail/`): the landing page for a specific course or language, mirroring `ExamDetailScreen`'s shape — a back-arrow `Scaffold` with Edit and Delete icons in the top bar, plus a prominent **Study Resources** button that navigates on to the actual study-resource list. `CourseDetailScreen`'s edit dialog covers name, icon, *and* exam type (a `DropdownMenu`, same pattern as `ExamFormDialog`'s `ExamTypeField`) — the dropdown deliberately excludes the Language Exam type, since a `Course` row tagged with it would be unreachable from any Courses/Study Resources bucket (Language Exam Courses lists `Language` rows, not `Course` rows). `LanguageDetailScreen`'s edit dialog is name-only. Both screens own all editing/deleting for their entity now — the list screens that lead here (`CourseListByTypeScreen`, `LanguageExamCoursesScreen`, `LanguageListScreen`) only render name(+icon) and navigate on tap, with no inline edit/delete affordances of their own, applied uniformly across every entry point.
- **Language Exam Courses is its own screen, not an alias for the Languages drawer screen**: tapping "Language Exam Courses" navigates to `LanguageExamCoursesScreen` (`ui/languagelist/LanguageExamCoursesRoute`) — a back-arrow `Scaffold` drill-down (mirroring `CourseListByTypeScreen`'s chrome) that reuses the existing `LanguageListViewModel` as-is (it has no route/nav dependency, so the same add-language logic runs under a second, independent navigation entry) rather than navigating into the drawer's `LanguageListRoute`. This was a deliberate correction: the two screens show the same underlying `Language` data and both let you drill into a language's detail/Study Resources, but they are different back-stack destinations reached from different parents (Courses vs. the drawer) — exactly like `CourseListByTypeScreen` is never the literal `CourseListScreen`. The drawer's own "Languages" item (`LanguageListScreen`) stays as a separate, always-available shortcut to the same data, by the user's explicit choice — it now also routes a tapped row to `LanguageDetailScreen` rather than editing inline, for consistency with every other entry point.
- **Study Resources** (named "Study Sources" until renamed throughout by explicit request — see `MIGRATION_8_9` below): a per-course/per-language resource list, and also its own top-level drawer destination (`StudyResourceHomeRoute`/`StudyResourceHomeScreen` in `ui/studyresource/`, positioned in the drawer between Courses and Languages). `StudyResourceHomeScreen` reuses `CourseListViewModel` and renders the identical four-exam-type-bucket menu as `CourseListScreen`, but tapping a bucket routes to its **own** drill-down destinations — `StudyResourceCourseListByTypeRoute(examTypeId)` and `StudyResourceLanguageExamCoursesRoute` — not the Courses flow's `CourseListByTypeRoute`/`LanguageExamCoursesRoute`, even though all four routes render the exact same `CourseListByTypeScreen`/`LanguageExamCoursesScreen` composables (and, for the course case, the same route-agnostic `CourseListByTypeViewModel`, which reads `examTypeId` by argument name via `savedStateHandle.get<Long>("examTypeId")` rather than a route-typed `toRoute<>()`, precisely so it can back both routes; `LanguageExamCoursesScreen` already had no route dependency at all, reusing `LanguageListViewModel` as-is). The reason for the split: tapping a course/language from the *Courses* flow still goes to `CourseDetailRoute`/`LanguageDetailRoute` (edit/delete/exam-type live there), but tapping one from the *Study Resources* flow must skip Detail entirely and land directly on `CourseStudyResourceRoute`/`LanguageStudyResourceRoute` — Study Resources is meant as a shortcut straight to the study-resource list, not a second path into Detail. This was originally built wrong (Study Resources aliased the Courses routes, so it always detoured through Detail first) and was corrected by giving the two entry points separate routes, per the standing "separate parents into the same data need separate routes" rule already applied to `LanguageExamCoursesRoute` vs `LanguageListRoute`. `StudyResource`: id, `name`, nullable `courseId` and nullable `languageId` (exactly one populated per row, `CASCADE` on delete of the owner), nullable `type: StudyResourceType?` (`QUESTION_BANK`/`LECTURE_TEXTBOOK`/`PRACTICE_EXAM`/`LECTURE_NOTES` — the deferred "resource types will be specified later" finally landed; nullable in the schema so pre-existing resources read as "Not set," but the Add dialog requires picking one before Save enables), and nullable `publisher: String?` (optional, collected but never required). One shared presentational composable, `ui/studyresource/StudyResourceListContent.kt`, backs the two terminal screens/ViewModels — `CourseStudyResourceScreen`/`CourseStudyResourceViewModel` (`CourseStudyResourceRoute(courseId)`) and `LanguageStudyResourceScreen`/`LanguageStudyResourceViewModel` (`LanguageStudyResourceRoute(languageId)`) — both back-arrow `Scaffold`s, titled with the owning course/language's name, reached via either their respective Detail screen's Study Resources button (Courses flow, and the Languages drawer shortcut) or directly from the Study Resources drill-down (Study Resources flow). Its Add dialog (`StudyResourceFormDialog`, also in `StudyResourceListContent.kt`) shows the owning course/language name **read-only** ("Subject: Geometry" — already auto-attached via `courseId`/`languageId`, this just surfaces it rather than asking the user to re-pick it), plus Name, a required `StudyResourceTypeField` dropdown (same `ExamTypeField`-style Box+DropdownMenu pattern used throughout the app), and an optional Publisher field. Tapping any resource row navigates to `ui/studyresourcedetail/StudyResourceDetailScreen.kt` (`StudyResourceDetailRoute(studyResourceId)`) — a back-arrow `Scaffold` owning Edit/Delete/Duplicate (Name/Type/Publisher all editable there; Subject is not, same "fixed at creation" precedent as other identity-defining FKs), mirroring the Course/Language Detail pattern applied uniformly across the app — so the list rows themselves carry no inline edit/delete anymore, just name + a type subtitle. Duplicate (`Icons.Filled.ContentCopy`, between Edit and Delete) is `StudyResourceDetailViewModel.duplicateStudyResource(copySuffix, onCreated)`: inserts a new `StudyResource` copying the current one's `name + copySuffix` (`sr_copy_suffix`, a localized `" (Copy)"`/`" (Kopya)"` — quoted in both `strings.xml` files to keep the leading space, same whitespace gotcha as `home_welcome_prefix`), `type`, `publisher`, and owning `courseId`/`languageId`, then copies every attached `StudyResourceTopic` row (`topicId`, `testCount`, `questionCount`, `orderIndex` all carried over as-is) onto the new resource's id; `onCreated` navigates to the new resource's own `StudyResourceDetailRoute` (pushed on top, so back returns to the original) via `TargetNavHost`.
- **Topics**: per-course, reusable within that course, tracking how many tests/questions a Question Bank study resource covers for each topic — and, since multiple Question Banks (different publishers) can attach to the same topic, how many exist for that topic in total across publishers. `Topic` (`data/local/entity/Topic.kt`): id, name, `courseId` (FK, non-null, `CASCADE` — unlike `StudyResource`'s dual-nullable courseId/languageId, a Topic has exactly one parent, so it follows `Section.examId`'s simpler non-null-FK shape). Topics are **Course-only** — Language-based Question Banks never show a Topics section, by deliberate scope decision, not an oversight. The join entity `StudyResourceTopic` (`data/local/entity/StudyResourceTopic.kt`) carries the actual counts, one row per (question bank, topic) pair: `studyResourceId`/`topicId` (both FK `CASCADE`, unique compound index), `testCount`/`questionCount: Int` (default 0) — mirrors `ExamCourse`/`SectionCourse`'s join-table shape exactly. `TopicDao.getTopicTotalsByCourseId(courseId)` is the cross-publisher aggregate query (`LEFT JOIN` + `SUM` + `COALESCE`, so a topic with no attachments still shows 0/0 rather than disappearing); `StudyResourceTopicDao.getByTopicId(topicId)` is the per-publisher breakdown query, `getByStudyResourceId(studyResourceId)` lists topics attached to one resource.

  On `StudyResourceDetailScreen`, when `studyResource.type == QUESTION_BANK && studyResource.courseId != null`, a "Topics" section appears: an "Add Topic" checklist-picker-with-create-inline dialog (a direct transplant of `ExamDetailScreen`'s `AddCourseDialog`/`addCourse`/`addExistingCourses` pattern, scoped to the resource's `courseId` instead of `(userId, examTypeId)`) attaches topics at 0/0, then each attached-topic row is tappable — this used to open a small inline "Edit counts" dialog directly on this screen, but as of Practice Sessions below it now navigates to its own `TopicProgressScreen` instead (Test Count/Question Count editing and detaching the topic both moved there too, see below).

  A new **Topics** button on `CourseDetailScreen` (next to Study Resources) leads to `ui/topiclist/TopicListScreen.kt` (`TopicListRoute(courseId)`) — full CRUD, mirroring Course/Language: FAB creates a topic directly (find-or-create, case-insensitive), each row shows the topic name plus its cross-publisher totals as a subtitle, tap navigates to `ui/topicdetail/TopicDetailScreen.kt` (`TopicDetailRoute(topicId)`) — Edit (rename) / Delete in the top bar (Delete cascades `StudyResourceTopic` rows via FK, confirmed on-device), and a body showing the prominent aggregate total above a per-resource breakdown list (`"<resource name> — <publisher or \"No publisher\">"` + that resource's own counts) — **this breakdown is the feature's actual payoff**, answering "how many tests/questions exist for this topic across different publishers." Each breakdown row taps through to `StudyResourceDetailRoute`, so you can jump from a topic straight into any contributing publisher's question bank.
- **Practice Sessions**: by explicit request ("track how many tests and questions they've solved... categorized as solved or unsolved... how many minutes or hours it took"), the app now records actual study progress against a `StudyResourceTopic`'s target counts, not just the targets themselves. `PracticeLog` (`data/local/entity/PracticeLog.kt`): id, `studyResourceTopicId` (FK to `StudyResourceTopic`, `CASCADE` — a `StudyResourceTopic`'s unique `(studyResourceId, topicId)` index makes its own `id` a sufficient anchor, the same shape `StudyResourceTopic` itself uses to point at `StudyResource`/`Topic`), `testsSolved`/`solvedCount`/`unsolvedCount: Int` (solved = correct, unsolved = wrong — confirmed via clarifying question, not attempted-vs-skipped), `durationMinutes: Int` (combined from separate Hours/Minutes number inputs at save, not stored as two fields), `loggedAt: Long`. Total questions and a Turkish-exam-style **net score** (`solvedCount - unsolvedCount / 4.0`, confirmed via clarifying question — the standard doğru − yanlış/4 formula) are **derived at display time, never stored**, the same "compute in Kotlin, don't persist a redundant field" convention `TopicDetailViewModel.totalTestCount`/`totalQuestionCount` already established. `PracticeLogDao.getByStudyResourceTopicId` orders by `loggedAt DESC`.

  Tapping a topic row in `StudyResourceDetailScreen`'s Topics list now navigates to `ui/topicprogress/TopicProgressScreen.kt` (`TopicProgressRoute(studyResourceTopicId)`, a back-arrow `Scaffold` drill-down, same exception as `TopicDetailScreen`) instead of opening the old inline "Edit counts" dialog — chosen over an inline dialog by explicit request, since a session log needs room for a running aggregate and history list, not just two numbers. This screen shows: the target counts (`"Hedef: X test · Y soru"`), an aggregate line summing every logged session for this topic+resource pair (tests, solved/unsolved, net score, total time as "Xh Ym"/"Xsa Ydk"), a "+ Log Session" action opening `LogSessionDialog` (five digit-filtered numeric fields — Tests Solved, Solved, Unsolved, Hours, Minutes — the same numeric-input template `StudyResourceTopic`'s original Test/Question Count fields set), and a session history list (most recent first) where tapping a row reopens the same dialog pre-filled for editing, with a "Delete this session" action inside it — mirroring the old `EditTopicCountsDialog`'s "Remove topic" placement precedent (an editable row opens a small editor rather than exposing a bare inline delete icon). The screen's Edit/Delete top-bar icons now own what `EditTopicCountsDialog` used to: Edit opens a small dialog for the target Test Count/Question Count (unchanged fields, just relocated), and Delete detaches the topic from the resource — its confirmation dialog explicitly names how many logged sessions will cascade-delete with it (`topicprogress_detach_message`, reads the live session count), since that's a real, easy-to-miss consequence of the FK `CASCADE` that wasn't previously in scope for that confirmation. `StudyResourceDetailViewModel.updateTopicCounts`/`removeTopic` were deleted (moved into the new `TopicProgressViewModel`) rather than kept as dead code. Confirmed on-device against the real 13-topic Chemistry Question Bank (schema v12 migrating cleanly from v11): logged a session (3 tests, 20 solved, 5 unsolved, 1h30m) and saw the aggregate line read "3 test · 20 doğru · 5 yanlış · Net 18,75 · Toplam 1sa 30dk" (net correctly computed as 20 − 5/4 = 18.75, Turkish comma-decimal formatting picked up automatically from the active locale the same way `SimpleDateFormat` already does), the session row appeared with the correct date/summary, tapping it reopened the edit dialog exactly pre-filled, Cancel left the data untouched, the target-counts edit dialog and the detach confirmation (correctly reading "...deletes 1 logged session...") both opened correctly, and a WAL-inclusive `.db` pull showed the exact expected row.

  By explicit follow-up request ("it has to limit test and question count to what topic has... if there is a session user has to see what is left"), `TopicProgressScreen` also shows a **Remaining** line (`label_remaining_counts`, target minus every logged session's own tests/solved+unsolved so far, `coerceAtLeast(0)`) right under the existing Target line, and `LogSessionDialog` clamps input live so the cumulative total across all of a topic's sessions can never exceed its target — a 15-test topic can't reach 16 no matter how many sessions get logged against it. The max for each field is computed excluding the *other* sessions' contributions relative to whichever one is being edited (`sessions.filter { it.id != editingSession?.id }.sumOf { ... }`), not the grand total — otherwise editing an existing session would double-count its own prior value against itself and refuse to let it keep its current numbers. Tests Solved clamps directly against its own remaining max; Solved and Unsolved clamp against *each other* since their sum (not either one alone) is what has to stay under the remaining question count — each field's `onValueChange` reads the sibling field's current value to compute its own allowed ceiling. A small `labelSmall` hint under each capped field (or pair) states the live limit (`"En fazla %1$d"` / `"Doğru + yanlış en fazla %1$d"`). Confirmed on-device: Remaining correctly read "Kalan: 12 test · 127 soru" against a target of 15/152 with one logged session of 3/25 already counted; typing "99" into Tests Solved (remaining max 12) clamped live to "12"; Cancel discarded the test input without saving anything, leaving the original session data untouched.

  Progress was still only visible one screen deep (on `TopicProgressScreen` itself), so by explicit follow-up request it now also surfaces a level earlier, in two places. First, `StudyResourceDetailScreen`'s topic rows show **remaining alongside target** (`counts_remaining_of_target` = `"%1$d/%2$d tests · %3$d/%4$d questions"`, e.g. "12/15 tests · 127/152 questions") instead of just the raw target — backed by a new `StudyResourceTopicDao.getByStudyResourceIdWithProgress` query (`LEFT JOIN practice_logs` + `GROUP BY study_resource_topics.id`, the same fan-out-safe shape `TopicDao.getTopicTotalsByCourseId` already uses) and a new `StudyResourceTopicWithProgress` projection; the existing `getByStudyResourceId`/`StudyResourceTopicWithTopic` are untouched since `CourseDetailViewModel`'s CSV export/import still depend on their exact shape. Second, `CourseStudyResourceScreen` (the course's Study Resources list, e.g. "Chemistry", listing its Question Banks) gained a **totals header above the list**: target test/question totals summed across every Question Bank under the course, a Remaining line, and the same solved/unsolved/net/duration aggregate line format already used per-topic — reusing the *exact same* `label_target_counts`/`label_remaining_counts`/`progress_aggregate` string resources, no new strings needed for the header itself. Two new aggregate queries back it: `StudyResourceTopicDao.getTargetTotals(studyResourceIds)` and `PracticeLogDao.getProgressTotals(studyResourceIds)`, both taking a `List<Long>` of the course's Question Bank resource ids (resolved by filtering `studyResourceDao.getByCourseId` by `type == QUESTION_BANK` in Kotlin — the same convention `CourseDetailViewModel.fetchQuestionBankExportRows` already established — not a new SQL type filter), and both deliberately joining only to the "one" side of their relationship (`study_resource_topics` has no fan-out risk either way) to avoid the row-multiplication bug a naive single combined query would have. `StudyResourceListContent.kt` gained an optional `summary: (@Composable () -> Unit)? = null` slot rendered above the list/empty-state (both now `Modifier.weight(1f)` inside a `Column`, so the summary keeps its natural height and the list correctly gets the remainder); `LanguageStudyResourceScreen` passes nothing, so it's visually unchanged — consistent with Topics/Practice Sessions staying Course-only, since a `Topic` requires a non-null `courseId`. Confirmed on-device against the real Chemistry course (4 Question Bank resources, including a known pre-existing duplicate): the header read "Hedef: 535 test · 5487 soru / Kalan: 532 test · 5462 soru / 3 test · 20 doğru · 5 yanlış · Net 18,75 · Toplam 1sa 30dk", cross-checked to the exact number via a direct WAL-inclusive `.db` pull and `sqlite3` aggregate query on the same 4 resource ids (535/5487 target, 3/20/5/90 progress — both matched exactly); the "Kimya Bilimi" topic row correctly read "12/15 tests · 127/152 questions"; French's `LanguageStudyResourceScreen` (a real course-less Language) showed no header, confirming the Course-only scope held.

  A gap surfaced through real use, reported directly: "today i solve a test from tyt mathematics sayı kümeleri but when i want to add session, it has to ask tests are how many questions... i solver 1 test and this test has 10 question. i did 4 correct 3 wrong and the others re empty." Tests vary in question count (3 tests = 31 questions one day, 29 the next), and `solvedCount + unsolvedCount` alone can't tell how many were left blank or how much of a topic's target the test actually consumed — a 10-question test with 4 correct/3 wrong only ever registered as "7 questions," silently losing the 3 blanks. Fixed (schema v15) by adding `PracticeLog.questionCount: Int?` (nullable — pre-existing sessions have no recorded value and can't be backfilled, `MIGRATION_14_15`'s plain `ALTER TABLE ... ADD COLUMN`, same shape as `MIGRATION_12_13`'s `study_resources.questionCount`), mirroring `PracticeExamEntry`'s existing questionCount/blank shape exactly rather than inventing a new one. `LogSessionDialog` gained a required Question Count field (Save disabled until it's set) between Tests Solved and Solved/Unsolved, live-clamped against the topic's own remaining target the same way Tests Solved already was; Solved/Unsolved's mutual clamp changed from the topic-wide remaining to *this session's own* entered Question Count (`PracticeExamEntryDetailScreen`'s `EditEntryResultDialog` correct/wrong-against-`entry.questionCount` pattern, transplanted here), with a live `label_blank_count` hint (`questionCount - solved - unsolved`, reused as-is from Practice Exams) showing the blank count as you type. Editing a legacy session (no recorded `questionCount`) prefills the field from `solvedCount + unsolvedCount` as a starting guess rather than leaving it blank/stuck.

  `solvedCount + unsolvedCount` had been used as a stand-in for "questions this session covered" in three other places beyond the dialog, all switched to `questionCount ?: (solvedCount + unsolvedCount)` (Kotlin) / `COALESCE(questionCount, solvedCount + unsolvedCount)` (SQL) so legacy sessions keep their old approximation while every new session (required going forward) uses the real number: `StudyResourceTopicDao.getByStudyResourceIdWithProgress`'s `loggedQuestions` sum (feeds `StudyResourceDetailScreen`'s topic-row remaining/target line), `PracticeLogDao.getProgressTotals`/`getProgressByTopicIds` (gained a `totalQuestionsLogged` column on `PracticeLogTotals`/`TopicProgressByTopic`, feeding `CourseStudyResourceScreen`'s totals header and the Progress CSV export's `remainingQuestions` respectively). Accuracy deliberately stayed `solved`/`unsolved`-only everywhere (blanks were never part of accuracy anywhere in the app — Practice Exam's weak-topics ranking, the Statistics screen — so this wasn't extended to match). `progress_aggregate` and `session_row_summary` both gained a blank count, inserted right after unsolved, matching where Practice Exam's `entry_result_summary` already places it; `label_questions_remaining_hint` (superseded by the live blank hint) was deleted as dead code rather than left unused. Confirmed on-device against the user's own real "Sayı Kümeleri" session (Eyl 14, 2026) under TYT Mathematics: the app migrated cleanly from v14; editing that real session to add Question Count 10 immediately showed the live "Boş: 3" hint, and saving updated the aggregate to "1 test · 4 doğru · 3 yanlış · 3 boş · Net 3,25 · Toplam 0sa 43dk" and Remaining from "8 test · 74 soru" to the correct "8 test · 71 soru" (81 − 10, not 81 − 7); the same corrected remaining (71/81) propagated to the Question Bank's topic row on `StudyResourceDetailScreen` and to Matematik's `CourseStudyResourceScreen` totals header ("Kalan: 135 test · 1258 soru", down from the course's prior 1268 target by exactly the real 10); a freshly-exported "TYT - Progress.csv" showed the Sayı Kümeleri row's Remaining Questions column as 71, cross-checked directly against the pulled file. A fresh add-session test (not just editing the legacy one) confirmed the new-session path too: typing 999 into Question Count live-clamped to the real remaining max (71), Doğru 50/Yanlış 40 clamped Yanlış down to 21 (50+21=71 exactly, "Boş: 0"), Save correctly refused to insert anything while Question Count was empty (verified by tapping Save on an all-empty dialog and confirming no new row appeared), and the completed test insert/delete round-tripped Remaining to 0 and back to 8 test/71 soru cleanly, with no crash (`adb logcat -b crash` clean) across the whole session. The test insert was deleted afterward — not real study data to keep.
- **Course icons**: `ui/common/CourseIcons.kt` is a curated, fixed list (~17) of `material-icons-extended` `ImageVector`s keyed by string (Functions, Science, Biotech, MenuBook, Language, Palette, Add, etc.) — added specifically because `material-icons-core` (already a dependency) only has ~48 generic action icons, nothing subject-appropriate. `CourseIcons.iconFor(key)` resolves a nullable key to an icon, falling back to a default (`MenuBook`) when unset — this is the one place in the app an icon is chosen from a fixed palette rather than free text. `ui/common/CourseIconPicker.kt` has the picker grid (`CourseIconPicker`, used in the Courses add/edit dialog) and the small rendering helper (`CourseIconAvatar`, used everywhere a course is listed: Courses screen, `ExamDetailScreen`'s course pool and add-course checklist, `SectionDetailScreen`'s assigned/available course lists) — every one of those reads `Course.icon` / the `courseIcon` column added to the `ExamCourseWithCourse`/`SectionCourseWithCourse` join projections, so a course's icon looks the same wherever it's referenced, matching how course *names* already worked.
- **ExamCourse**: join table — an exam's own course pool. Always present regardless of `hasSections`, because sections pick their courses *from* this pool.
- **Section**: only exists when `Exam.hasSections`. Name + own `date` (a sectioned exam's "exam date" is really N section dates, not one field).
- **SectionCourse**: join table — a section's subset of its parent exam's course pool. The add-course-to-section UI is a **checklist picker** over `ExamCourseDao.getByExamId(exam.id)` minus what's already assigned, not create-if-missing (unlike the exam-level course pool) — a section can't have a course the exam doesn't already have.

`ExamDetailScreen`'s Courses and Sections lists are each collapsible, defaulting to **closed** — reported as looking "crowded" once an exam had more than a couple of courses. `SectionHeader` (private composable in `ExamDetailScreen.kt`) takes optional `expanded`/`onToggleExpand` params and renders a `KeyboardArrowDown`/`KeyboardArrowUp` chevron next to "+ Add" when supplied; the two lists track independent `coursesExpanded`/`sectionsExpanded` `remember { mutableStateOf(false) }` state and only compose their `items(...)` when expanded. Not persisted — collapses again on next screen visit, by design (no reported need yet to remember the open/closed state across navigation). The **Courses section itself is hidden entirely on Language Exam type exams** (computed via `isLanguageExam`, hoisted above the `LazyColumn` in `ExamDetailScreen`) — a Language Exam has no course pool, just its single `languageId` field (already shown/editable elsewhere), so the course-oriented "Add" flow doesn't apply there; this was reported as a bug ("can't add an existing language course to Goethe A2") before being traced to the real issue — the Courses section shouldn't have been showing on Language Exams at all. Tapping a course row in this pool navigates to `CourseDetailRoute(courseId)` via `onCourseClick` (added to `ExamDetailScreen`'s signature and wired in `TargetNavHost`) — this was missing entirely until reported as a bug (the row had a remove button but no click handler). The Language Exam analogue: `ExamSummary`'s "Language: X" line is clickable (`Modifier.clickable`, only when a language is actually set — "No language set" stays inert) and navigates to `LanguageDetailRoute(languageId)` via a third `onLanguageClick` param on `ExamDetailScreen`, giving a Language Exam the same "tap through to the thing's own detail/Study-Resources page" behavior that courses already have on other exam types. The clickable affordance wasn't discoverable as plain text, so just the language name (not the "Language: " label) is styled as a link — blue + underlined — via `buildAnnotatedString`/`SpanStyle(color = MaterialTheme.colorScheme.primary, textDecoration = TextDecoration.Underline)`, the second place in the app a single string mixes styles inline (the first being `HomeScreen`'s bolded preferred name).

The exam-level Courses list is also hidden — differently from the Language Exam case — **when the exam has sections**: the "Courses" header and its "+ Add" button still render (that's still how a course gets attached to the exam's pool that sections pick from — see `ExamCourse`/`SectionCourse` above), but the collapse chevron and the actual list of attached courses don't, since a sectioned exam's courses are meant to be seen under their section, not duplicated at the top level too. `SectionDetailScreen`'s course rows (`AssignedCourseRow`) got the same "tappable → `CourseDetailRoute`" fix as `ExamDetailScreen`'s pool did, closing the same gap (a remove button with no click handler) — so Section → Course Detail → Study Resources is now a complete chain, matching Exam → Course Detail → Study Resources for non-sectioned exams.

- **Course-level Question Bank CSV export**: `CourseDetailScreen` has a third top-bar action, Export (`Icons.Filled.Share`, between Edit and Delete), by explicit request after manually entering a large Chemistry Question Bank felt tedious enough to want a backup. It's a one-way, scoped export — not a general backup/restore (that stays out of scope; see above) — covering exactly one course's `QUESTION_BANK`-type `StudyResource`s and their attached topics. `CourseDetailViewModel.fetchQuestionBankExportRows(onResult)` filters `StudyResourceDao.getByCourseId` to `type == QUESTION_BANK` in Kotlin (no new SQL — same "filter by type in Kotlin" convention `StudyResourceDetailScreen` already uses for its own Topics-section gate) and flat-maps each resource's `StudyResourceTopicDao.getByStudyResourceId` rows into `[resourceName, publisher, topicName, testCount, questionCount]` string rows — one flat table, one row per (resource, topic) pair, no per-course aggregation. `data/export/CourseQuestionBankCsvExporter.kt` (`object`, matching the `PasswordHasher`/`PreferredNameResolver` single-purpose-utility shape) writes it as CSV to `context.cacheDir/exports/<sanitized course name> - Question Bank.csv` — a UTF-8 BOM is written first (`ef bb bf`), since Excel silently mojibakes Turkish characters (ç, ğ, ı, ö, ş, ü) in a BOM-less UTF-8 CSV otherwise; a small `csvField()` escapes any value containing a comma/quote/newline. The filename uses the course's raw stored name, not its Turkish display translation, matching the existing "never let a translated string overwrite/represent the underlying data" rule. `ui/common/ShareFile.kt`'s `shareCsvFile()` wraps the written file in a `content://` `Uri` (via a new `FileProvider`, `AndroidManifest.xml`'s first `<provider>` entry + `res/xml/file_paths.xml`'s `<cache-path name="exports" .../>`) and launches `Intent.ACTION_SEND` (`type = "text/csv"`) through `Intent.createChooser`. This is the app's first file-export/sharing code of any kind — no new library was needed (`FileProvider` ships in `androidx.core`, already transitively present via `core-ktx`), and it doesn't touch the offline hard constraint: a share-sheet handoff is local IPC, not network access, so no `INTERNET` permission was added. Confirmed on-device against the real Chemistry course: the share sheet correctly recognized the file as Excel-openable, and the pulled CSV had all 47 rows across the course's three real Question Bank resources (Miray Yayınları/eis Yayınları/ÜçDörtBeş) with exact counts, Turkish `Konu`/`Test Sayısı`/etc. headers (localized like every other UI string), correct BOM bytes, and correct quoting for topic names that themselves contain a comma (e.g. "Asitler, Bazlar ve Tuzlar").

The Export's counterpart, **Import**, was added right after by explicit follow-up request — a fourth `CourseDetailScreen` top-bar icon (`Icons.Filled.FileOpen`, between Export and Delete) that opens Android's document picker (`ActivityResultContracts.OpenDocument()`, `arrayOf("*/*")` — deliberately permissive since real file providers tag `.csv` under inconsistent MIME types) and re-imports the app's own export format back in, merging into the current course. `data/export/CourseQuestionBankCsvImporter.kt`'s `parse()` is a hand-written, quote-aware CSV row parser (handles embedded commas/quotes/newlines the same way the exporter escapes them, strips a leading BOM) that skips the header row and maps each remaining row to a `QuestionBankCsvRow` (resource name, publisher, topic, test count, question count) — malformed rows (too few fields, blank resource/topic name) are silently dropped rather than aborting the whole import. `CourseDetailViewModel.importQuestionBankCsv(csvContent, onResult)` merges deliberately, not destructively: a resource is matched by **(name, publisher)** rather than name alone, since two real resources in this app's own data can legitimately share a name with different publishers (confirmed against the real "TYT Kimya Soru Bankası" appearing twice under different publishers) — a match reuses the existing `StudyResource`, no match creates a new one (always `QUESTION_BANK` type, since that's the only type Export ever emits); a topic is matched by name case-insensitively within the course (same `addTopic`-style find-or-create pattern used throughout Topics); an existing (resource, topic) attachment has its counts **overwritten** by the CSV's values (by explicit request — the CSV is treated as the source of truth, e.g. for restoring a backup or bringing in edits made outside the app), while a new attachment is appended at the end of that resource's order. The result is reported back via a `QuestionBankImportOutcome` sealed type (`Imported(QuestionBankImportResult)` with counts of rows/resources/topics/attachments created vs. updated, or `EmptyOrInvalid` when nothing parsed) resolved to display text only in the Composable's result `AlertDialog` — the same "ViewModel emits a typed outcome, Composable calls `stringResource()` on it" split `ui/common/ErrorMessage.kt` already established, since a plain callback lambda isn't a composable context. Confirmed on-device with a hand-built test CSV covering all three merge paths at once (an existing resource+topic to overwrite, a new topic on an existing resource, and a brand-new resource) via the real system file picker (pushed to `/sdcard/Download/`, picked from Recents) — the result dialog and a WAL-inclusive DB pull both confirmed the exact expected counts (1 new resource, 1 new topic, 2 new attachments, 1 updated).

Two mistakes were made and caught while testing this feature, both from the same root cause — forgetting the screenshot-to-device-coordinate `*1.17` scaling factor (see the device-testing memory) while cleaning up test data afterward: a `Cancel` tap landed on a different dialog's **"Remove topic from this resource"** action instead, silently detaching three real, pre-existing topics (Mol Kavramı, Tepkime Türleri, Karışımlar) from the real "TYT Kimya Soru Bankası"/Miray Yayınları resource. Caught immediately by a follow-up DB check (13 topics expected, only 10 present); fixed by re-attaching all three via the existing checklist-picker and manually restoring their exact original counts (cross-checked against the untouched duplicate resource discussed below, which still had all 13). Second: this same testing session also turned up a **pre-existing duplicate** "TYT Kimya Soru Bankası"/Miray Yayınları resource (id 10) with an identical `createdAt` timestamp to the original (id 6) and all 13 original topics intact — i.e., a full copy, most likely made via the Duplicate feature and then manually renamed to drop the "(Kopya)" suffix. This was left untouched (not this session's data to fix) but is worth knowing: **because Import matches by (name, publisher), a course with two identically-named-and-published resources is fundamentally ambiguous to Import** — a CSV row for that pair will always resolve to whichever one the DB query happens to return first, never the other. This is a known sharp edge, not a bug to fix reactively; flagged here in case it causes confusion in a future session.

Import crashed the whole app when the picked file came from Google Drive: `ContentResolver.openInputStream(uri)` threw an uncaught `FileNotFoundException: File is virtual` for a Drive-native document (e.g. a real Google Sheet, not an actual `.csv`) — Drive can hand back "virtual" documents that have no raw byte stream of their own, and an ActivityResult callback lambda has no natural place to catch that before it reaches `ActivityThread` and takes the process down. Reported directly ("when i try to import csv file its crashes") and confirmed via `adb logcat -b crash`, which pointed straight at the `openInputStream` call in the import launcher. Fixed in `ui/common/ShareFile.kt`'s new `readTextFromUri(context, uri)`: wraps the read in try/catch, and on failure falls back to `contentResolver.getStreamTypes(uri, "*/*")` + `openTypedAssetFileDescriptor` (preferring a `text/*` alternate MIME type if the document declares one) before giving up and returning null — `CourseDetailScreen`'s import launcher now calls this instead of the inline `openInputStream`, so any read failure (Drive virtual doc or otherwise) surfaces as the existing "Couldn't read that file" dialog rather than crashing. Confirmed on-device: re-imported a genuine local CSV (regression check, still works), then deliberately picked a real Google Sheet from Drive (the exact scenario that crashed before) — no crash, the file-unreadable dialog appeared instead, and `adb logcat -b crash` showed no new `FATAL EXCEPTION` after the fix (only the two pre-fix crash entries remained).

A **Topics** drawer entry was added right after, mirroring **Study Resources**'s shape (by explicit request) but adapted for the fact that Topics are Course-only (see above) — it has no Language Exam Courses bucket at all, since a Language has no Topics list to route to. `ui/topiclist/TopicHomeScreen.kt` reuses `CourseListViewModel` like `StudyResourceHomeScreen`/`CourseListScreen` do, but filters `examTypes.filterNot { it.name == LANGUAGE_EXAM_TYPE_NAME }` before rendering the bucket list — three buckets (University Entrance/High School Entrance/Vocational), never four. Tapping a bucket goes to the new `TopicCourseListByTypeRoute(examTypeId)`, which — like `StudyResourceCourseListByTypeRoute` — is just another route backing the same route-agnostic `CourseListByTypeScreen`/`CourseListByTypeViewModel` (already reading `examTypeId` by argument name rather than a route-typed `toRoute<>()`, precisely so a third route could reuse it with zero ViewModel changes). Tapping a course there navigates straight to the *existing* `TopicListRoute(courseId)` — no new terminal screen was needed, since `ui/topiclist/TopicListScreen.kt` already existed as `CourseDetailScreen`'s "Topics" button destination; the drawer entry is purely a shortcut that skips Course Detail, same shortcut relationship Study Resources already has to Course Detail. `AppShell.kt`'s drawer gained a `Topics` item (`Icons.Filled.Topic`) between Study Resources and Languages, with the same divider-on-both-sides treatment as every other drawer entry; `ShellNavigation` gained `onNavigateTopics`. Confirmed on-device: the drawer shows Exams/Courses/Study Resources/Topics/Languages/Home in that order; tapping Topics shows exactly 3 buckets (no Language Exam Courses); drilling into University Entrance → Quantitative → Chemistry lands directly on "Chemistry Topics" (the same screen `CourseDetailScreen`'s Topics button reaches), skipping Course Detail entirely; the Study Resources flow's own bucket menu is unaffected and still shows all 4 buckets including Language Exam Courses.

By explicit follow-up request, the Study Resources flow's bucket wording was changed from "...Courses" to "...Resources" throughout (e.g. "University Entrance Exam Resources"), since that flow is about browsing toward study resources, not courses — the Courses and Topics flows deliberately kept saying "...Courses" (you're still picking a *course* there, for course-editing or topic-drilling respectively), so this needed to be flow-specific rather than a blanket string change. `CourseListByTypeScreen` (shared by all three flows' bucket drill-downs) gained a `titleFormatRes: Int = R.string.course_type_bucket_title` parameter, and `LanguageExamCoursesScreen` (shared by Courses' and Study Resources' Language Exam buckets) gained a `titleRes: Int = R.string.label_language_exam_courses` parameter — both default to the original "Courses" wording, and only `TargetNavHost`'s `StudyResourceCourseListByTypeRoute`/`StudyResourceLanguageExamCoursesRoute` composables override them (`course_type_bucket_title_resources` = `"%1$s Resources"`/`"%1$s Kaynakları"`, `label_language_exam_resources` = "Language Exam Resources"/"Dil Sınavı Kaynakları"). `StudyResourceHomeScreen`'s own bucket-menu `ListItem`s were switched to the `_resources` string directly, since that screen has no shared-composable parameter to thread through. Applies to both the bucket menu itself and the screen you land on after tapping it, per explicit request, so the wording is consistent end-to-end within the flow. Confirmed on-device: all four Study Resources buckets ("University Entrance/High School Entrance/Language/Vocational Exam Resources") and the screens reached by drilling into them now say "Resources"; the Courses flow (and Topics flow) still say "Courses" throughout, unaffected.

The exam-type bucket rows in `CourseListScreen`, `StudyResourceHomeScreen`, and `TopicHomeScreen` (e.g. "University Entrance Exam Courses"/"...Resources") first got a static trailing chevron as a pure visual affordance, then — by explicit follow-up correction ("i want what we do at sections... you can tap them for a shortcut") — were reworked to match `ExamDetailScreen`'s Sections/Courses collapsible pattern instead: the chevron (`Icons.Filled.KeyboardArrowDown`/`KeyboardArrowUp` in an `IconButton`, matching `SectionHeader`'s exact icon choice) now **expands the bucket in place**, listing its courses (or, for the Language Exam bucket, its languages) directly beneath the header row; tapping one of those inline rows is a **shortcut** straight to that course/language's destination, skipping the bucket's own full-screen list entirely. Tapping the header row's own body (not the chevron) still opens that full screen exactly as before — the `IconButton` is a separate clickable region nested inside the `ListItem`'s `trailingContent`, the same "inner clickable wins the hit-test, outer row-clickable still fires everywhere else" pattern already proven by `StudyResourceDetailScreen`'s drag-handle-vs-row-tap split. Expand state is `remember { mutableStateMapOf<Long, Boolean>() }` keyed by `examType.id`, not persisted — same "collapses again on next visit" precedent as `ExamDetailScreen`. `CourseListViewModel` gained a `courses: StateFlow<List<Course>>` (the user's *entire* course pool, unfiltered) so a plain `allCourses.filter { it.examTypeId == examType.id }` inside the non-composable `examTypes.forEach { }`/`LazyListScope` body can supply each expanded bucket's rows — the per-bucket Flow-collection approach was deliberately avoided, since collecting a Flow requires a composable context and the outer loop over exam types is not one (same "group by a raw key outside the composable, only call composables inside `item { }`" rule already documented for the Quantitative/Verbal category groupings). The Language Exam bucket (Courses and Study Resources flows only — Topics excludes it, as always) reuses `LanguageListViewModel.languages` as-is, matching how `LanguageExamCoursesScreen` already reuses that same ViewModel unmodified. Shortcut destinations mirror each flow's existing "skip Detail" philosophy: Courses → `CourseDetailRoute`/`LanguageDetailRoute`; Study Resources → `CourseStudyResourceRoute`/`LanguageStudyResourceRoute` directly; Topics → `TopicListRoute` directly. An empty expanded bucket shows a small `bucket_expand_empty_hint` line ("Nothing here yet — tap above to add one.") rather than silently showing nothing, since a zero-row expand can otherwise look like the tap did nothing. Confirmed on-device across all three flows and both bucket types (Course and Language): expand/collapse toggles correctly and independently per bucket, the header row's body still opens the full screen (with its own Quantitative/Verbal grouping and "+Add" FAB) alongside the new expand affordance, and every shortcut row lands exactly where its full-screen equivalent already would.

### Home dashboard (clock + upcoming exams) and exam-list countdowns

By explicit request ("a widget on the home page showing the time, day, and date... the number of days remaining until exams... both in a section on the home page and on the 'My Exams' page... For exams with sections... instead of 'sections,' it could display the specific sections"), `HomeScreen` and `ExamListScreen` both gained day-countdown UI, backed by shared helpers rather than duplicated math:

- `ui/common/DateFormatting.kt` gained `daysUntil(epochMillis)`/`startOfTodayMillis()` — both calendar-day-truncated (midnight-to-midnight `Calendar` math, not raw `millis / 86400000`, which would give the wrong answer depending on time-of-day) so "today" is always exactly `0` regardless of what time it currently is. `ui/common/ExamCountdown.kt`'s `daysUntilLabel(epochMillis)` (`@Composable`, since it resolves localized strings) turns that day count into "Today" / "Tomorrow" / "%d days left" / "Passed" — shared by both screens below rather than reimplemented per screen.
- **`HomeScreen`** gained a `GroupedCard` clock — time (`HH:mm`, 24-hour) + weekday/date (`EEEE, MMM d, yyyy`) — ticking once a minute via a `LaunchedEffect` loop that sleeps until the next minute boundary (`delay(60_000 - (now % 60_000))`), not every second; a study tracker doesn't need second-accurate time, and this avoids needless recomposition. Below it, an "Upcoming" `GroupedCard` (hidden entirely when there's nothing to show) lists every future-dated exam/section as its own row, **flattened into one sorted timeline** — confirmed via clarifying question over the alternative (one row per exam, showing only the nearest section for sectioned ones) — since a sectioned exam has no single date of its own, only its sections do, so treating "TYT" and "AYT" as independent upcoming events (each sorted by its own date) is more informative than collapsing them into their parent exam's row. Past-dated exams/sections are filtered out of this widget entirely (an "upcoming" list showing already-passed dates isn't useful) — `HomeViewModel.upcomingEvents` builds this from `ExamDao.getAllWithTypeByUserId` (reused as-is, already the shape `ExamListViewModel` uses) combined with the new `SectionDao.getByUserId`.
- **`ExamListScreen`**'s `ExamRow` gained a second supporting-content line — the countdown — under the existing type/date text. For a non-sectioned exam with a date set, this is just `daysUntilLabel(examDate)`. For a sectioned exam, the old literal `"Sectioned"` label (`exam_row_sectioned`, now deleted — confirmed unused elsewhere before removing) is replaced with the exam's actual section names joined by comma (e.g. "TYT, AYT", or `exam_row_no_sections` = "No sections yet" if the exam has none), and the countdown line becomes `"<days phrase> · <nearest section name>"` (`exam_countdown_section`) — computed from whichever attached section has the soonest date, confirmed via clarifying question over the alternative (section names with no countdown at all) since a countdown to *something* is more useful than none, even though a sectioned exam has no single date of its own. `ExamListViewModel` gained `examsWithSections: StateFlow<List<Pair<ExamWithType, List<Section>>>>` (`combine` of the existing `exams` flow with a new `allSections` flow, grouped by `examId` in Kotlin) to back this — the same "fetch broadly, group/aggregate in Kotlin" shape already established by `StatisticsViewModel`/`CourseDetailViewModel`'s CSV export, rather than a new per-exam SQL query. `SectionDao.getByUserId(userId)` (joined through `exams`, ordered by `examId` then `orderIndex`) is the one new DAO query, shared by both this and `HomeViewModel` above.

Confirmed on-device against the real Goethe A2 (Nov 30, 2026, non-sectioned) and YKS (TYT/AYT sections) exams: Home's clock card showed the correct live time and today's date; its Upcoming card listed "Goethe A2 · 75 days left", "YKS – TYT · 276 days left", "YKS – AYT · 277 days left" in that sorted order; the Exams list showed the matching 75-day countdown under Goethe A2 and correctly read "University Entrance Exam · TYT, AYT" followed by "276 days left · TYT" for YKS; tapping through to both exams' detail screens from the list still worked with no regression.

By explicit follow-up request, each Upcoming row is now tappable — `UpcomingEvent` gained a nullable `sectionId` (set only for section events, alongside the `examId` it already carried, which for a section event is the *parent* exam's id) so `HomeScreen` can resolve the same "is this a section or a bare exam" branch the row's own label already encodes: `onSectionClick(sectionId)` when set, `onExamClick(examId)` otherwise — the identical `Long -> Unit` callback shapes and `TargetNavHost` wiring (`SectionDetailRoute`/`ExamDetailRoute`) `ExamListScreen` already uses, not a new pattern. Confirmed on-device: tapping "Goethe A2" opens that exam's own `ExamDetailScreen`; tapping "YKS – TYT" skips the parent YKS exam entirely and opens the **TYT section's** own `SectionDetailScreen` directly (Date: Jun 19, 2027, Quantitative/Verbal course groups) — the section-vs-exam distinction resolves correctly in both directions.

Per explicit follow-up ("didn't like the UI... make the text underlined and when user tap it hover to another color like the app color preference"), the row's label was restyled as a link instead of plain tappable text, without touching the card's own background — `UpcomingEventRow` (private composable, `HomeScreen.kt`) always underlines the label (`TextDecoration.Underline`) and, via a `MutableInteractionSource` + `collectIsPressedAsState()` + `animateColorAsState`, animates its color between `onSurface` and `MaterialTheme.colorScheme.primary` while pressed (reverting on release) — `primary` is the same role `pinnedColorScheme`/dynamic-color already drives from the App Color Preference (see above), so the press color always matches whatever accent is currently selected, with zero extra plumbing. The row's default ripple (`indication = null` on `clickable`) was dropped in favor of this color animation as the sole press feedback, matching the "link" metaphor rather than the app's usual full-row ripple. Confirmed on-device: all three rows render underlined; holding down on "Goethe A2" (captured mid-press via a backgrounded long `input swipe`) showed the label switch to the app's accent color while held and revert on release, with the card's tonal background completely unchanged; a normal tap still navigated to the correct exam/section detail screen with no regression.

### Planner

A general-purpose agenda layered on top of the exam-prep model, by explicit request — its own `PlannerEvent` entity (schema v17), not a repurposing of anything above, so it can hold things with no exam/course relationship at all (e.g. a birthday) alongside things that do (a study session tied to a specific course/topic).

- **`PlannerEvent`** (`data/local/entity/PlannerEvent.kt`): id, nullable `userId` (FK `CASCADE`, same per-user scoping as `Exam`/`Course`), `title`, optional `notes`, `category: PlannerEventCategory` (`GENERAL`/`BIRTHDAY`/`STUDY`), `startDate` (epoch millis, start-of-day — the first/only occurrence), optional `startMinuteOfDay` (untimed/all-day when null) + `durationMinutes`, an optional recurrence (`recurrenceUnit: RecurrenceUnit?` — `DAY`/`WEEK`/`MONTH`/`YEAR` — plus `recurrenceInterval`, a comma-separated ISO-weekday `recurrenceWeekdays` string for `WEEK` only, and an optional inclusive `recurrenceEndDate`), and — `STUDY` only — nullable `courseId`/`topicId` (FK `SET_NULL`, `topicId` optional even when `courseId` is set, the same "exactly one populated" shape `StudyResource` already uses for its own dual-nullable FKs). Birthday is always implicitly yearly — `PlannerEventFormDialog` hides the recurrence controls entirely for that category rather than showing a locked-but-visible one, and forces `recurrenceUnit = YEAR`/`recurrenceInterval = 1` on save regardless of what the (hidden) fields hold. `durationMinutes` is never typed directly — `PlannerEventFormDialog` shows a required **End Time** `TimeInput` right below the **Start Time** one (both labeled, `planner_field_start_time`/`planner_field_end_time`) whenever "Set a time" is on, and `durationMinutesBetween()` derives the stored value from the two selected times (wrapping past midnight — `diff + 24*60` — if end is before start, so it's never zero or negative). This replaced an earlier free-typed "Duration (minutes)" text field that could be left blank or entered as `0` — a real on-device event ("Tarkovsky Movies Event") had silently saved that way and displayed as a degenerate "17:00 – 17:00" until re-saved through this fixed dialog; requiring an explicit end-time selection (not just a number) makes that state impossible to create going forward, by explicit request ("if there is no ending hour... user has to select it as well").
- **`PlannerEventCompletion`** (schema v17): id, `plannerEventId` (FK `CASCADE`), `occurrenceDate` (epoch millis, which specific occurrence — not the event — was marked done), `completedAt`. A unique `(plannerEventId, occurrenceDate)` index backs an idempotent toggle (`OnConflictStrategy.IGNORE` on insert, delete-by-key to un-mark) — completion is per-occurrence, so marking one Monday of a "every Monday" recurring event done doesn't affect any other Monday.
- **Recurrence math lives entirely in `ui/planner/PlannerOccurrences.kt`'s `occurrencesInRange(event, rangeStart, rangeEnd)`** — the one function every agenda view (Week/Month/Year) and the Home preview card all call. It fast-forwards mathematically to just before `rangeStart` (ceiling-division arithmetic per unit) rather than looping one step at a time from the event's anchor date, so an event created years ago still resolves instantly for a one-week window; a `MAX_CANDIDATES` backstop (10,000) guards only against a corrupt row (e.g. `recurrenceInterval` somehow 0), never hit by any realistic window. `YEAR` recurrence on a Feb 29 anchor falls back to Feb 28 in non-leap years. `java.time` is used throughout this file and its siblings (`PlannerDates.kt`, `PlannerHomeViewModel.kt`, `PlannerEventFormDialog.kt`) — **the only place in the app that does** — via `PlannerDates.kt`'s `Long.toLocalDate()`/`LocalDate.toStartOfDayMillis()` boundary functions; core library desugaring (`isCoreLibraryDesugaringEnabled = true`, `coreLibraryDesugaring(libs.desugar.jdk.libs)`) was added to `app/build.gradle.kts` specifically to make this safe on `minSdk 24` — the build change the Statistics section (above) had earlier flagged as "otherwise unneeded" and avoided by sticking to `Calendar`/`SimpleDateFormat`; Planner is what finally needed it. `PlannerOccurrences.kt`'s logic has its own unit test suite, `app/src/test/java/com/ridvan/target/ui/planner/PlannerOccurrencesTest.kt` — the app's first, since everything before this was verified on-device rather than with a test runner (recurrence math has enough edge cases — interval spacing, weekday sets, Feb 29 — to be worth a real regression net beyond on-device spot checks).
- **`PlannerHomeScreen`** (`ui/planner/`, its own drawer entry — `Icons.Filled.CalendarMonth`, `label_planner` — positioned as a standalone item directly below the collapsible "Study" group, not inside it, since a general-purpose planner isn't exam-prep-specific the way Exams/Courses/Study Resources/Topics/Statistics/Languages are) has a Week/Month/Year `SegmentedToggle` (`PlannerViewMode`) plus a prev/next period stepper. `PlannerHomeViewModel.agendaItems` merges three sources into one sealed `PlannerAgendaItem` list for whatever date range the current mode+anchor imply (`visibleRange`): `PlannerEvent` occurrences (via `occurrencesInRange`), non-sectioned exams with a date, and sections with a date — reusing `ExamDao.getAllWithTypeByUserId`/`SectionDao.getByUserId` exactly as Home's own Upcoming card does, so an exam/section shows up on both without a second query shape. Week view is one `GroupedCard` per day (`PlannerWeekAgenda.kt`); Month view is a calendar grid with a dot under any day that has items, tapping a day shows that day's agenda below the grid in the same per-day-card composable (`DayAgendaCard`, shared with Week); Year view is 12 collapsible month sections (`PlannerYearList.kt`, only the current month starts expanded — expanding all 12 by default was judged an unscannable wall of text, collapsing all would hide the one section most people open Yearly to see first). Tapping an `EventOccurrence` row opens `PlannerEventFormDialog` pre-filled for editing (Delete inside it, labeled "Delete this series" instead of "Delete this event" when the event recurs, since deleting one `PlannerEvent` row removes every occurrence, not just the tapped one); tapping an `ExamEntry`/`SectionEntry` row navigates straight to that exam's/section's own existing detail screen, the same "read-only entries jump to the real thing" precedent `StudyLinkLine` already established for a Study category event's attached course/topic.
- **Home preview**: `HomeScreen` gained a third `GroupedCard`, "Planner," below the existing clock and Upcoming cards — **always shown**, unlike Upcoming (which hides when empty), since this card doubles as Home's quick entry point into the full Planner even before anything's been logged. It lists up to 6 upcoming `PlannerEvent` occurrences within the next 13 days (`ui/planner/PlannerHomePreview.kt`'s `upcomingEventOccurrences()` — windowed, not just capped-after-sort, so a single far-future occurrence is never even a candidate — keeps `java.time` inside `ui/planner` per the boundary above, handing `ui/home` back a plain `PlannerPreviewOccurrence` with an epoch-millis date), each row with an inline `Checkbox` (wired straight to `HomeViewModel.togglePlannerOccurrenceDone`, the same insert/delete-by-key toggle `PlannerHomeViewModel` uses) and an underlined title styled exactly like `UpcomingEventRow`'s press-color-animates-to-accent link, tapping through to the full Planner (`shellNavigation.onNavigatePlanner`) rather than opening an edit dialog inline — Home doesn't have that dialog's course/topic-picker plumbing, and "tap to see more" is enough for a glance-level widget. Deliberately **events-only**, not merged with exam/section dates — those are already the existing Upcoming card's job, and duplicating them into a second card here would just repeat the same rows. A card-header "Open Planner" link (underlined, primary-colored) gives the same full-screen jump even when the list is empty. Confirmed on-device against the developer's own real events ("Football match with friends," "Tarkovsky Movies Event"): the card correctly showed today's event with the right time range; toggling its checkbox from Home updated the completion state and round-tripped correctly when checked again; the "Open Planner" link and a tapped event title both opened the full Planner (Week view, correct Sep 14–20 range) with the real data intact; back navigation returned cleanly to Home.
- **Dates and time-of-day, by explicit follow-up request** ("add the dates of upcoming events as well", then "if its today or tomorrow add the clock as well"): both the Upcoming and Planner cards' trailing column gained the actual `formatDate()`-formatted calendar date as a second line under the existing relative `daysUntilLabel()` text (`UpcomingEventRow`/`PlannerPreviewRow`, `HomeScreen.kt`) — neither row showed a real date before, only "X days left"/"Today". For a Planner row specifically, a third line — the event's `"HH:mm – HH:mm"` time range — appears under the date, but **only** when `daysUntil(...)` is `0` or `1` (Today/Tomorrow) and the event is timed (not all-day); further-out events' time-of-day isn't shown, by the same explicit scoping. `PlannerPreviewOccurrence` carries this as a pre-resolved nullable `timeRangeLabel` field, computed in `upcomingEventOccurrences()` by reusing `PlannerWeekAgenda.kt`'s existing `timeRangeLabel(event)` helper (changed from `private` to `internal` rather than duplicated) — the exam/section Upcoming card has no equivalent, since `Exam`/`Section` have no time-of-day field to show. Confirmed on-device against the real "Tarkovsky Movies Event" (today, 17:00 start): both cards showed "Sep 20, 2026" under "Today," and the Planner card additionally showed "17:00 – 18:00" (after the end-time fix below); "Mum's Birthday" (4 days out, all-day) correctly showed its date with no time line.

### App icon

Two selectable launcher icons — `ic_launcher`/`ic_launcher_round` (white/default) and `ic_launcher_blue`/`ic_launcher_blue_round` (blue) — each an adaptive icon (foreground/background XML pair in `drawable`/`drawable-xxxhdpi`, plus legacy raster fallbacks at every mipmap density). Both were generated from flat-background design PNGs (developer's desktop `target-app-designs` folder) by chroma-keying the background color to transparent for the foreground layer — there's no Android Studio Image Asset step in this workflow, so any future icon variant needs the same treatment (sample the actual flat background pixel, not a corner pixel, which can be an anti-aliasing outlier). Switching icons is done via two `<activity-alias>` entries in the manifest (`AppIconDefault`, `AppIconBlue`, both `targetActivity=".MainActivity"`), toggled at runtime through `PackageManager.setComponentEnabledSetting(..., DONT_KILL_APP)` from `SettingsViewModel.setUseBlueAppIcon()` — `MainActivity` itself no longer carries the `LAUNCHER` intent-filter, only the aliases do. The choice persists in `AppPreferences.useBlueAppIcon` (`StateFlow<Boolean>`, same pattern as `isDarkMode`) so the Settings toggle reflects the right side on reopen. Settings shows this as a White/Blue segmented toggle styled like `PreferredNameSelector`'s First/Middle segments (not a plain `Switch`) — the second deliberate reuse of that visual pattern outside its original component. Known quirk: switching aliases on an app already installed via adb can cause the launcher to show a stale/duplicate icon until the app is uninstalled and reinstalled — a launcher-cache artifact, not a data bug.

### App Color Preference

By explicit request, after the user created eight new drawer-banner design variants (`target-app-designs`, colors: darkgray, green, pink, powderpink, purple, red, yellow, alongside the existing blue), Settings gained a way to pick one — and, per a follow-up clarification, the chosen color changes **both** the drawer banner image **and** the app's whole accent color (buttons, FAB, Study Resources/Add buttons, etc.), not just the banner. `data/local/AppPreferences.kt`'s `BannerColor` enum (`BLUE`/`DARK_GRAY`/`GREEN`/`PINK`/`POWDER_PINK`/`PURPLE`/`RED`/`YELLOW`) carries each option's exact hex (sampled from the banner PNGs via `System.Drawing.Bitmap.GetPixel()`, no ImageMagick/PIL available in this environment), its drawable resource, and its label string; persisted the same `SharedPreferences`-backed `StateFlow` shape as `useBlueAppIcon`/`isDarkMode`. `ui/theme/Theme.kt`'s `TargetTheme` takes a `bannerColor` param: `BLUE` preserves the app's original Material You dynamic-color behavior **exactly** unchanged (no regression to the default experience), any other color calls a new `pinnedColorScheme(seed, darkTheme)` that starts from `lightColorScheme()`/`darkColorScheme()` and overrides only `primary`/`onPrimary`/`primaryContainer`/`onPrimaryContainer` to the seed color (contrast text chosen via `Color.luminance() >= 0.5f`) — so switching away from Blue is the only thing that opts out of dynamic color, and switching back fully restores it. `ui/shell/AppShell.kt` reads the preference directly from `TargetApplication` (matching `MainActivity`'s existing direct-read pattern, avoiding threading a new param through nine screen call sites) to pick the drawer banner drawable. Settings shows all eight as a 4×2 grid of circular `ColorSwatch`es (a selected/unselected border plus a check icon, tinted for contrast the same way), not a plain toggle — confirmed via clarifying question that all eight should be offered, not a 2-way toggle. A known, accepted side effect: since `pinnedColorScheme` only overrides the primary-family roles, non-Blue selections show Compose Material3's baseline purple-tinted background/surface underneath the chosen accent (the developer was asked directly and chose to leave this as-is rather than also override background/surface to neutral). Confirmed on-device: all eight swatches render with correct colors/contrast, selecting a color immediately updates both the drawer banner image and the accent color on buttons/toggles/FABs app-wide, selecting Blue fully restores the original dynamic-color look, and the choice persists across app restarts.

### Statistics

The final piece of the Practice Sessions arc: a dedicated `ui/statistics/StatisticsScreen.kt`/`StatisticsViewModel.kt` (new drawer entry, `Icons.Filled.BarChart`, between Topics and Languages) that aggregates all of a user's logged sessions in one place, filterable by exam/course/topic and slicable by time period, so a student can see themselves improving rather than only ever viewing one topic's numbers in isolation. By explicit request: "feature engineer" derived metrics beyond raw sums (accuracy %, pace), and a real trend chart, not just numbers.

`PracticeLogDao.getAllForUser(userId)` (backed by a new `PracticeLogWithTopicContext` projection — `@Embedded PracticeLog` + `topicId`/`topicName`/`courseId`) fetches the user's **entire** practice-log history once, joined up through `study_resource_topics`→`topics`→`courses` and scoped by `courses.userId`. Every filter, time-window, and metric computation happens in Kotlin over that one list — deliberately not per-period SQL queries — consistent with the app's established "aggregate/filter in Kotlin over a fetched list" convention (`TopicDetailViewModel`'s totals, `TopicProgressScreen`'s aggregate line, `CourseDetailViewModel`'s CSV export all do this already). Realistic data volume here is one user's own manually-logged sessions, not a scale that needs database-side aggregation.

**Filters** cascade exactly like the original three-filter design called for: Exam ("All Exams" default) → narrows the Course dropdown to that exam's attached courses via the **existing** `ExamCourseDao.getByExamId` (no new reverse Course→Exam query needed) → Course ("All Courses") → Topic ("All Topics", enabled only once a course is picked, via existing `TopicDao.getByCourseId`). Applying a filter is a plain Kotlin `.filter` on the fetched rows' own `courseId`/`topicId` fields (and, for the exam filter, whether `courseId` is in the selected exam's attached-course-id set).

**Period**: `StatsPeriod` (`DAILY`/`WEEKLY`/`MONTHLY`/`ALL_TIME`, a 4-segment toggle styled like the existing `SettingsToggleSegment`s, default `ALL_TIME`). Bucketing/windowing uses `java.util.Calendar` (matching `DateFormatting.kt`'s existing `Date`/`SimpleDateFormat` usage — `minSdk = 24` with no core-library-desugaring configured, so `java.time` isn't safely available without an otherwise-unneeded build change). Recent-window sizes: Daily → last 14 days, Weekly → last 8 weeks, Monthly → last 6 months, All-time → bucket by month across the full history (capped at the most recent 24 buckets). Selecting a period re-windows **both** the summary numbers and the chart, not just the chart — so switching to Weekly/Monthly genuinely narrows what you're looking at, per the user's explicit ask to be able to "see improvements per weekly or even monthly."

**Derived metrics** (`StatsSummary`, computed from whatever rows are in the current filter+period window): the existing net-score formula reused as-is, plus two new ones confirmed via clarifying question — **accuracy** (`solved / (solved+unsolved) * 100`, `stat_accuracy_label`, 0 when no questions attempted) and **pace** (`durationMinutes / (solved+unsolved)` and `/ testsSolved`, `stat_pace_label`, "X.X min/question · X.X min/test").

**Trend chart**: a private `TrendBarChart` composable using Compose `Canvas` (no new dependency — confirmed no charting library exists in `app/build.gradle.kts`) draws one **stacked** bar per period bucket (solved segment + unsolved segment), so the chart doubles as a visual accuracy trend, not just a volume trend. Bucket labels render in a `Row` of `Text` underneath the canvas rather than via `drawText` (kept the canvas code simple); to avoid overcrowding, only every Nth label renders (`buckets.size / 6` step) while every bar still gets its correct width/position.

**Breakdown list** groups the current filter+period window's rows by `topicId` (the finest available granularity), each row `"<topic name> (<course name>)"` — `courseDisplayName()`-translated, matching how a course name is shown read-only everywhere else in the app — with its own tests/solved/unsolved/duration (`session_row_summary`, reused as-is), tap-through to `TopicDetailRoute` (the cross-publisher topic page — correct destination since a topic can span multiple resources/publishers).

Confirmed on-device: with one real logged session (2 tests, 30 solved, 7 unsolved, 40 min) on a real Chemistry topic, the summary read "2 test · 30 doğru · 7 yanlış · Net 28,25 · Toplam 0sa 40dk / Doğruluk: %81 / Hız: 1,1 dk/soru · 20,0 dk/test" — every number hand-verified correct (28.25 = 30−7/4, 81% = 30/37, 1.1 = 40/37, 20.0 = 40/2); the stacked bar chart rendered the single bucket correctly on Daily/Weekly/Monthly/All-time with no crash on the single-data-point case, each with correctly-windowed x-axis labels; the Exam/Course/Topic filters correctly narrowed the numbers (including to a real empty-data duplicate course, showing the empty state correctly); and the breakdown row's tap-through landed on the correct `TopicDetailScreen`. One pre-existing (not introduced this session) minor issue was found while testing, then fixed by explicit follow-up request: `TopicDetailScreen`'s contribution list (`StudyResourceTopicDao.getByTopicId`) ordered only by `study_resources.name ASC`, with no secondary sort key — so when multiple resources share the exact same name (a known real duplicate in this app's own data — two "TYT Kimya Soru Bankası" resources, one pair even sharing the same publisher too), SQLite's unspecified tie order meant the row order wasn't guaranteed stable across recompositions. Fixed by adding `study_resources.publisher ASC, study_resources.id ASC` as secondary/tertiary sort keys — `id` alone would have been sufficient for determinism, but ordering by publisher first keeps same-named/same-publisher duplicates visually grouped rather than interleaved with the correctly-distinct-publisher rows. Confirmed on-device: the same four-resource "Kimya Bilimi" contribution list (two Miray entries, one eis, one ÜçDörtBeş) now renders in the identical order across repeated navigations and a full app relaunch (`adb shell am force-stop` + relaunch), where before the fix this same list's order was the actual bug this note originally flagged.

**Crash fixed (reported directly: "yesterday when i show it to the my friends its crashes and closes")**: when the Practice Session per-session question count feature (schema v15, see Practice Sessions above) added a `%4$d blank` placeholder to both `progress_aggregate` and `session_row_summary`, `TopicProgressScreen`'s and `CourseStudyResourceScreen`'s own usages were updated to pass it, but two usages inside this screen were missed — the Practice Sessions summary line and each `BreakdownRow`. Either one threw `IllegalFormatConversionException: d != java.lang.String` the instant a user opened Statistics with Practice Sessions selected and any real logged data present (empty-state skipped the crash, which is why it looked intermittent rather than every time). Confirmed via `adb logcat -b crash` going back to **09-14** — well before the interface redesign — so this long predates that work and isn't a redesign regression, despite surfacing again mid-redesign-testing (one of the crashes logged was mistaken in the moment for the developer using the phone concurrently; it was actually this bug). Fixed by giving `StatsSummary` and `TopicBreakdownEntry` their own `blank` field, computed the same way `TopicProgressScreen` already does (`questionCount?.let { (it - solved - unsolved).coerceAtLeast(0) } ?: 0`, summed across the windowed rows), and passing it at both call sites. Confirmed on-device: Statistics now opens cleanly with real Practice Session data, correctly showing the blank count in both the summary line and every breakdown row; Practice Exams and all four period toggles also verified crash-free.

### Practice Exams

By explicit request, the previously out-of-scope `PRACTICE_EXAM` `StudyResourceType` (an enum value that already existed but had no behavior behind it) was built out — but the shape went through a real redesign mid-implementation after the developer tried the first version on-device. The **first** version put a single question count directly on the `StudyResource` and modeled results as many dated "attempts" of that one exam (schema v13). After trying it, the developer clarified the real mental model: a Practice Exam `StudyResource` (e.g. "Denemeler" under Chemistry, publisher "ÜçDörtBeş") is a **container/directory** holding many individually-named exams ("Deneme 1", "Deneme 2", ...), each with its **own** question count — entered only when that specific named exam is added, not when the container itself is created. `MIGRATION_13_14` undoes the wrong `questionCount` column on `study_resources` via the standard SQLite drop-column-via-recreate pattern (create `study_resources_new` without it, `INSERT ... SELECT` the real columns across, drop and rename — matching `MIGRATION_8_9`'s existing recreate precedent, chosen specifically because real Chemistry Question Bank data lives in that table and had to survive untouched) and drops/recreates the attempt-shaped tables (`practice_exam_attempts`/`practice_exam_topic_results`, which only ever held this session's own throwaway test data) into the corrected shape: `practice_exam_entries` (`PracticeExamEntry`: id, `studyResourceId` FK cascade, `name`, `questionCount`, `correctCount`/`wrongCount`/`durationMinutes` — all default 0, since a named exam is created with just a name+count and its result filled in later) and `practice_exam_entry_topic_results` (`PracticeExamEntryTopicResult`, unchanged shape from the first version, just re-keyed to `practiceExamEntryId`). A named exam holds exactly **one** editable result, not a history of dated attempts (confirmed via clarifying question) — retaking it means overwriting its numbers, the same "tap to open a small editor" pattern as `StudyResourceTopic`'s own counts. The **blank/unanswered count (boş)** is always derived (`questionCount - correctCount - wrongCount`, `coerceAtLeast(0)`), never stored — the same "compute in Kotlin, don't persist a redundant field" convention as net score/accuracy elsewhere.

Per a second round of on-device feedback, the feature is **inline on `StudyResourceDetailScreen`**, not behind a button to a separate list screen — the developer explicitly didn't want an extra tap to see the practice exams ("i do not like to tap the button and saw the practice exams. just list them at the detail page"). When `studyResource.type == PRACTICE_EXAM`, the screen shows a "Practice Exams" section (mirroring the existing Topics section's shape exactly): a "+ Add Exam" action opens a small dialog (Name + Question Count only — correct/wrong are deliberately not asked here, only entered once the exam is actually taken), an aggregate stats line once any exams exist (`practice_exam_entries_summary`: count, total correct/wrong, accuracy %, total duration — same derivation as `CourseStudyResourceScreen`'s target/progress header), and a list of entry rows (`"<name>" / "<correct>✓ / <wrong>✗ / <blank> boş"`). Tapping a row still drills into its own screen, `ui/practiceexam/PracticeExamEntryDetailScreen.kt` (`PracticeExamEntryDetailRoute(entryId)`) — editing one entry's result plus its optional per-topic breakdown needs more room than an inline row, the same "list inline, drill in for the deep editor" split `StudyResourceDetailScreen`'s Topics section already has with `TopicProgressScreen`. That screen's Edit action opens a Correct/Wrong/Hours/Minutes dialog with correct+wrong **capped** to the entry's own `questionCount` (same live-clamping convention as `LogSessionDialog`), showing the live-computed blank count as a hint; Delete cascades the entry's topic-result rows. Below, **only when the owning `StudyResource.courseId != null`** (Topics/topic-breakdowns stay Course-only, as everywhere else in the app), a Topic Breakdown section lets you optionally attach the course's topics (checklist-picker, same shape as `StudyResourceDetailScreen`'s Add Topic dialog) and edit each one's own Question Count/Correct/Wrong — the per-topic cap is against **that row's own** question count, not the overall entry's, a bug caught and fixed during on-device testing (the first pass wrongly capped against the entry's total).

**Statistics integration**: `StatisticsViewModel` gained a `StatsSource` toggle (`PRACTICE_SESSIONS`/`PRACTICE_EXAMS`, default `PRACTICE_SESSIONS` so existing behavior is unchanged) rendered as a second segmented toggle above the existing filters. Selecting Practice Exams swaps the data source to `PracticeExamEntryDao.getAllForUser`/`PracticeExamEntryTopicResultDao.getTopicAggregatesForUser` (same join-through-`study_resources`/`courses`-scoped-by-`userId` shape as the Practice Sessions source) — the Exam/Course filters still apply, the Topic filter is disabled for this source (an entry isn't inherently scoped to one topic, since its breakdown is optional and can span several), the summary/chart reuse the exact same components (just fed entries instead of logs, with "tests" now meaning "how many named exams"), and the breakdown list is replaced by a **"Topics to Work On"** ranking (`practice_exam_entry_topic_results` aggregated across all of a user's entries, sorted **ascending** by accuracy — the topics most worth reviewing float to the top), tapping through to the same `TopicDetailRoute` as everywhere else.

By the same follow-up request, the Statistics screen's **Exam filter dropdown now groups by exam type** (University Entrance Exam / High School Entrance Exam / Language Exam / Vocational Exam headers, each with its exams listed underneath, rendered via `examTypeDisplayName()`) instead of one flat list — mirroring the Courses screen's bucket grouping conceptually, though implemented as dropdown section headers rather than separate screens, since `DropdownMenu`'s content lambda is already a real composable context (no `LazyListScope`-style "compute the key outside, call `stringResource` only inside `item{}`" workaround needed here). `StatisticsViewModel.examGroups` groups the existing `ExamDao.getAllWithTypeByUserId` projection (`ExamWithType.examTypeName`) by type name, ordered against the canonical `ExamTypeDao.getAll()` seed order (University Entrance → High School Entrance → Language → Vocational) rather than first-seen order, so the grouping is stable and matches every other exam-type bucket ordering in the app.

Two more not-otherwise-obvious things worth remembering from this feature: (1) `adb shell input text` throws an uncaught `NullPointerException` on the device's input shell command when the string contains non-ASCII characters (e.g. "ÜçDörtBeş") — it fails the whole command with no text typed, not a partial send, so any future on-device typing of Turkish publisher/course names needs ASCII placeholders for scripted testing (real device typing is unaffected, this is an `adb`-shell-only limitation). (2) partway through testing this feature the developer was independently adding their own real example entries ("Kimya 1"–"Kimya 4" under a "Denemeler" container) during the same window a schema migration and a full reinstall happened — consistent with the same "check whether the developer is on the phone before assuming data loss is a bug" lesson documented earlier in this file: a first test container this session's own testing had created went missing across a reinstall, and rather than treating that as a migration bug, it was left alone (real course data was independently confirmed 100% intact across the same migration) since the missing row was only ever scratch verification data, and the developer's own real example data survived every subsequent rebuild correctly.

Real on-device use of the feature surfaced one more bug, reported directly ("i enter to kimya 1 7 correct but in topics i can add 1 wrong, it is a bug"): `PracticeExamEntryDetailScreen`'s Topic Breakdown editor only capped a topic row's correct/wrong against *that row's own* question count, never against how much of the **entry's** overall correct/wrong the other topic rows already accounted for — so a topic could record a wrong answer even when the entry itself had zero wrong answers overall. Fixed by computing three remaining budgets before opening the edit dialog (`entry.questionCount`/`correctCount`/`wrongCount` minus the sum of every *other* topic row's own values, `coerceAtLeast(0)` — the same "exclude self, subtract everyone else's share" pattern `LogSessionDialog` already uses for its own remaining-target cap) and clamping each field against `min(that row's own per-row cap, the entry-level remaining budget)`, with a `topic_result_correct_wrong_remaining_hint` label showing the live remaining correct/wrong under the fields. Confirmed on-device: with "Kimya 1" (7✓/0✗ overall) already having several topic rows entered before the fix (whose stale, now-inconsistent numbers were deliberately left alone — not this fix's job to retroactively repair old data), opening a not-yet-maxed-out topic row and typing "1" into Wrong correctly stayed clamped at "0" (the hint read "Doğru en fazla 3 · Yanlış en fazla 0"), and Cancel left the existing data untouched.

By explicit follow-up request, the course/language Study Resources list (`StudyResourceListContent.kt`, shared by `CourseStudyResourceScreen`/`LanguageStudyResourceScreen`) gained a **Type filter** — a dropdown (`StudyResourceTypeFilterField`, mirroring the shape of the existing `StudyResourceTypeField` picker but with a nullable "All Types" option prepended) that narrows the rendered list to one `StudyResourceType`. On the course flow specifically, the totals header **swaps to match the selected type** rather than always showing the Question Bank numbers regardless of what's filtered: `CourseStudyResourceViewModel.filteredStudyResources` applies the filter, and a new `practiceExamTotals` (backed by a new `PracticeExamEntryDao.getEntryTotals(studyResourceIds)` query → `PracticeExamEntryTotals`, the same "aggregate query taking a list of resource ids" shape as `StudyResourceTopicDao.getTargetTotals`/`PracticeLogDao.getProgressTotals`) feeds a Practice-Exam-flavored header (reusing `practice_exam_entries_summary`) when `PRACTICE_EXAM` is selected; selecting `LECTURE_TEXTBOOK`/`LECTURE_NOTES` (types with no numeric tracking model at all) shows no header; `QUESTION_BANK` or the "All Types" default keeps the original target/remaining/progress header unchanged. The Language flow gets the same Type filter for consistency (it never had a totals header to begin with, so nothing else changes there). Confirmed on-device on the real Chemistry course: "All Types" showed the unchanged Question Bank header above the full mixed list; switching to "Deneme Sınavı" narrowed the list to just "Denemeler" and swapped the header to "4 deneme · 11✓ / 1✗ · Doğruluk %91 · Toplam 0sa 16dk" (matching the practice-exam aggregate already shown elsewhere); switching to "Ders/Kitap" (no matching resources) correctly showed no header and the empty-state message.

### Progress CSV export (per-Section / per-Exam, for AI weekly planning)

By explicit request, a second CSV export was added alongside the existing course-level Question Bank export — this one is meant to be handed to an AI chat to generate a weekly study plan: "i want to export .csv file and with that file i want to ask maybe ai to make me an weekly solve plans... for next week the user ask again ai for weekly plan, ai can see what user solve past." Nothing about "weekly" tracking lives in the app itself — each export is just the current cumulative state, and the week-over-week comparison happens externally, in the AI conversation the user pastes two dated CSV exports into.

`data/export/ProgressExportRows.kt`'s `buildProgressExportRows(courses, topicDao, practiceLogDao)` is the shared row-builder (a plain suspend function, not tied to either screen's ViewModel, since `SectionDetailViewModel` and `ExamDetailViewModel` source their course list differently but need identical row output) — one CSV row per **(course, topic)**, the granularity the user explicitly asked for ("courses topic to more detail") so an AI can see per-topic struggle within a course, not just a per-course summary: `Course, Topic, Target Tests, Target Questions, Tests Solved, Correct, Wrong, Remaining Tests, Remaining Questions, Net, Accuracy %, Duration (minutes)`. Target counts reuse `TopicDao.getTopicTotalsByCourseId` (already powering `TopicListScreen`/`CourseStudyResourceScreen`); solved/correct/wrong is a **new** `PracticeLogDao.getProgressByTopicIds(topicIds)` query (→ new `TopicProgressByTopic` projection) since the existing `getProgressTotals` only sums by a list of `StudyResource` ids, not by raw `Topic` id — this one joins `topics → study_resource_topics → practice_logs` with `LEFT JOIN`/`COALESCE`/`GROUP BY topics.id`, the same shape `getTopicTotalsByCourseId` already uses for its own cross-publisher sum, one join further. Remaining/Net/Accuracy are derived in Kotlin from those two totals, reusing the exact existing formulas from `TopicProgressScreen`/`CourseStudyResourceScreen`. The course name is written **untranslated** (the raw stored name, not `courseDisplayName()`) — consistent with the existing CSV export's "never let a translated string represent the underlying data" rule, and sidesteps needing a Composable context inside the non-composable row-builder. Practice Exam (deneme) data is deliberately **not** included — confirmed via clarifying question, out of scope for this export since it doesn't share the same per-topic target/solved shape.

`data/export/ProgressCsvExporter.kt` is a small standalone object mirroring `CourseQuestionBankCsvExporter`'s exact shape (BOM-prefixed UTF-8, `csvField()` comma/quote/newline escaping duplicated rather than shared — matching this codebase's per-feature single-purpose-object convention) — writes to `cacheDir/exports/<sanitized scope name> - Progress.csv`. `SectionDetailViewModel.fetchProgressExportRows`/`ExamDetailViewModel.fetchProgressExportRows` each gather their own course list (`sectionCourseDao.getBySectionId`/`examCourseDao.getByExamId`, both already-fetched `StateFlow`s reused as-is) and delegate to the shared row-builder. `SectionDetailScreen.kt` gets a new top-bar Share `IconButton` (between Edit and Delete, same position `CourseDetailScreen`'s existing Export uses) unconditionally; `ExamDetailScreen.kt` gets the identical action but only rendered when `exam?.hasSections != true` — a sectioned exam's courses live under its sections, so the exam level has nothing course-scoped worth exporting on its own, matching the same `hasSections` gating already used for hiding the exam-level Courses list.

Confirmed on-device against the real YKS exam: the sectioned YKS exam's own detail screen correctly shows no export action at all (only Edit/Delete); drilling into its TYT section shows the Share icon between Edit and Delete; tapping it opens the share sheet with a valid "TYT - Progress.csv" (recognized as an Excel-openable CSV) — the share sheet was dismissed without picking a real contact/app, since this is the developer's actively-used phone, and the file was instead pulled and inspected directly via `adb ... run-as ... cat cache/exports/`. The pulled CSV had 96 correctly-formed rows across all four of TYT's real courses (Biology/Chemistry/Mathematics/Physics), the Turkish-localized header row, comma-containing topic names correctly quoted (e.g. `"Asitler, Bazlar ve Tuzlar"`), Turkish comma-decimal Net formatting ("0,00"), and topics with zero logged Practice Sessions correctly present with all-zero solved/correct/wrong columns rather than being dropped by the `LEFT JOIN`. The exam-level (non-sectioned) export path shares the identical `buildProgressExportRows` function and course-fetch pattern already exercised elsewhere in `ExamDetailViewModel`, so it wasn't separately re-verified against real non-sectioned course data (the only non-sectioned exam on-device is a Language Exam, which has no `Course` pool at all to export).

### Localization (English/Turkish)

Every user-facing string in the app lives in `res/values/strings.xml` (English, default) and `res/values-tr/strings.xml` (Turkish) — no hardcoded UI string literals remain anywhere in `ui/`. Common repeated words (Cancel/Save/Add/Delete/Name/Icon, etc.) are consolidated into shared `common_*`/`label_*` resources reused across screens rather than redefined per-dialog. **Trailing/leading spaces in a string resource are silently stripped by the XML parser unless the whole value is wrapped in double quotes** (`<string name="x">"prefix "</string>`, not `<string name="x">prefix </string>`) — bit the app once already (`home_welcome_prefix`'s space before the bolded name vanished, caught on-device) — any string a build-annotated/concatenated value depends on keeping edge whitespace needs this.

- **Free-text user data is never translated, with one deliberate exception: Course names.** Exam/topic/language/study-resource names, publishers, etc. always stay exactly as typed. Courses are different by explicit request: `ui/common/CourseNameDisplay.kt`'s `courseDisplayName(name: String): String` is a `@Composable` lookup, the same display-only-at-render-time pattern as `examTypeDisplayName()` — a fixed set of common subjects ("Geometry", "Biology", "Chemistry", etc.) translate for display (e.g. "Geometri", "Biyoloji", "Kimya"); any course name outside that list (a custom name, or "German A1"/"German A2"-style language-level course names that exist as leftover pre-Language-table data) just shows as typed, same as any other free text. Applied everywhere a course name is rendered read-only (list rows, checklists, screen/dialog titles, delete-confirmation messages) — **but deliberately not** to `CourseDetailScreen`'s `CourseEditDialog` `initialName` (the field you actually edit and save): translating that would mean saving the edit dialog without touching the name field silently overwrites the real English name with its Turkish display text, corrupting the underlying data. The drawer/menu word for the Courses feature itself was also renamed in Turkish from "Kurslar" to "Dersler" ("kurs" reads as an external training course, not an academic subject) — this only touched the `values-tr/strings.xml` chrome strings (`label_courses`, `dialog_add_course_title`, etc.), not the English default wording or any code/route/table naming.
- **`ExamType.name`/`LANGUAGE_EXAM_TYPE_NAME` stays English in the database and in all comparisons** — these are seeded DB rows (see `ExamTypeDao` above), not compile-time resources. `ui/common/ExamTypeDisplay.kt`'s `examTypeDisplayName(name: String): String` is a `@Composable` lookup (`when (name) { "University Entrance Exam" -> stringResource(...), ... }`) called only at the point of *display* — every place `examType.name`/`examTypeName` is rendered to the user wraps it in this; nothing ever compares against a translated value.
- **`ui/common/CourseIcons.kt`'s `CourseIconOption` stores a `@StringRes labelRes: Int`, not a raw `String`** — icon content descriptions need `stringResource()`, which requires a Composable context, but `CourseIcons.options` is a plain top-level `val` list; `CourseIconPicker` resolves `stringResource(option.labelRes)` at the point of use instead.
- **ViewModel-originated error messages** (login/register/profile-edit/switch-account validation) can't call `stringResource()` directly (`AndroidViewModel` isn't a Composable context) — they're modeled as `ui/common/ErrorMessage.kt`'s `ErrorMessage` enum (`INVALID_USERNAME_PASSWORD`, `USERNAME_TAKEN`, etc.), exposed as `StateFlow<ErrorMessage?>` from each ViewModel, and resolved to display text via `ErrorMessage.text()` (a `@Composable` extension) only in the Screen.
- **Category/type group-header labels vs. single-value labels use different fallback text for the unset case** — `ui/common/CourseCategoryLabel.kt` has two separate composables for this reason: `courseCategoryGroupLabel()` (null → "Uncategorized", used for the Quantitative/Verbal/Uncategorized group headers in `CourseListByTypeScreen`/`ExamDetailScreen`/`SectionDetailScreen`) and `courseCategoryValueLabel()` (null → "Not set", used for a single course's own category value in `CourseDetailScreen`) — conflating them into one function would show the wrong fallback word in one of the two places.
- **A composable that groups a `LazyColumn`'s items by category (or any enum) must NOT itself be `@Composable`** — `LazyListScope`'s builder body (the lambda passed to `LazyColumn { ... }`) is not a composable context, so calling `stringResource()`-based label functions there to compute group keys fails to compile. The fix used throughout (`ExamDetailScreen.examCourseGroups`, `SectionDetailScreen.sectionCourseGroups`, `CourseListByTypeScreen.courseGroups`): group by the raw enum/nullable key (e.g. `CourseCategory?`), keep the `mutableStateMapOf` expand-state keyed by that same raw enum (not by the translated display string, which would also be locale-dependent and require re-deriving the label a second time outside the composable `item {}` block just to read the map), and only call the label composable *inside* the `item { }` block where a real composable context exists.
- **Switching languages requires `Activity.recreate()`, not just a Compose-level override** — the first implementation tried wrapping `LocalContext` via `CompositionLocalProvider` in `MainActivity`'s `setContent`, which correctly re-localized ordinary screen content but silently left every `AlertDialog`/`DropdownMenu`/`DatePickerDialog` body and button text in the OS's real locale (confirmed on-device: a dialog's title — resolved in the caller's composition, before entering the dialog — translated correctly, while the exact same dialog's "Name"/"Icon"/"Cancel"/"Save" — resolved *inside* the dialog's own composable slots — did not). `Popup`/`Dialog` create their own Android `Window` and re-derive `LocalContext` from the real Activity context there, bypassing any ambient CompositionLocal override made further up the tree. The working fix: `MainActivity.attachBaseContext()` wraps the real base `Context` in a locale-adjusted `Configuration` (reading the persisted preference via a throwaway `AppPreferences(newBase)`), and `SettingsScreen` calls `LocalContext.current.findActivity()?.recreate()` (`ui/common/FindActivity.kt`) right after `viewModel.setAppLanguage(...)`. `recreate()` behaves like a configuration-change relaunch (same mechanism `AppCompatDelegate.setApplicationLocales()` uses internally) — confirmed on-device that it preserves navigation position, scroll/dialog state, and other Settings toggles (Dark mode, App Icon) across the switch, both directions.
- `AppPreferences.appLanguage: StateFlow<AppLanguage>` (`ENGLISH`/`TURKISH`) follows the same persisted-preference shape as `isDarkMode`/`useBlueAppIcon`, defaulting to Turkish only if the *system* locale is already Turkish at first launch, English otherwise. Setting it also calls `Locale.setDefault(...)` so `ui/common/DateFormatting.kt`'s `SimpleDateFormat("MMM d, yyyy", Locale.getDefault())` picks up correctly localized month names with no changes of its own needed. Settings shows this as an English/Turkish segmented toggle, the third reuse of the `PreferredNameSelector`/App-Icon segmented-toggle visual pattern.

### Navigation shell

`ui/shell/AppShell.kt` is the persistent chrome — a `ModalNavigationDrawer` (a 140dp-tall banner image, `drawable-xxxhdpi/drawer_banner.png` rendered via `Image(..., contentScale = ContentScale.Crop)`, clickable to go Home — then a collapsible "Study" group containing Exams, Courses, Study Resources, Topics, Statistics, Languages (see the Study drawer group paragraph below), then a standalone Planner item (see the Planner section above — deliberately outside the "Study" group, since a general-purpose planner isn't exam-prep-specific), then My Account, Settings, Help, and a "Home" item pinned to the drawer's bottom via a `ColumnScope` weight spacer, Help directly above Home) and a `TopAppBar` with a hamburger (opens the drawer) and a three-dot overflow menu. All eleven leaf drawer items (Exams, Courses, Study Resources, Topics, Statistics, Languages, Planner, My Account, Settings, Help, Home) carry a fixed `icon =` (`Icons.AutoMirrored.Filled.Assignment`, `Icons.AutoMirrored.Filled.MenuBook`, `Icons.Filled.Bookmark`, `Icons.Filled.Topic`, `Icons.Filled.BarChart`, `Icons.Filled.Translate`, `Icons.Filled.CalendarMonth`, `Icons.Filled.AccountCircle`, `Icons.Filled.Settings`, `Icons.AutoMirrored.Filled.Help`, `Icons.Filled.Home` respectively) — each was added after Home already had one, to match. Each item renders via a private `DrawerItem` composable (`Surface` + `Row`, `MaterialTheme.shapes.small`, `labelMedium` text, animated container/content color) rather than the stock Material3 `NavigationDrawerItem` — reported directly ("the rounded highlight is too big, maybe it can contain the text size... make text a little bit smaller and make them closer to each other"), since `NavigationDrawerItem`'s fixed ~56dp height and full-bleed selected pill read as oversized once the drawer grew to 8 destinations. **There is deliberately no divider between drawer destinations anymore** — the previous per-item `HorizontalDivider` (kept between every row, "so a future destination gets the same treatment") was removed as part of the same request, since dense compact rows read better with tight spacing (2dp) than with a full divider after each one; this is a reversal of that earlier convention, not an oversight. **Every** shell-level screen renders through it: `HomeScreen`, `ExamListScreen`, `CourseListScreen`, `StudyResourceHomeScreen`, `TopicHomeScreen`, `StatisticsScreen`, `LanguageListScreen`, `HelpScreen`, `MyAccountScreen`, `SettingsHomeScreen`, `UserEditScreen`, `SettingsScreen` all call `AppShell(navigation = shellNavigation, title = ...) { innerPadding -> ... }` instead of their own bare `Scaffold` — the menu is never absent on any of them, and none of them has a back-arrow icon anymore (getting back to Home is via the drawer, not a back button; the Android system back gesture still works normally through the nav back stack). `ShellNavigation` (also in `AppShell.kt`) bundles the `onNavigate*` callbacks (including `onNavigateStudyResources`/`onNavigateTopics`/`onNavigateStatistics`/`onNavigateHelp`/`onNavigateMyAccount`/`onNavigateSettings`/`onNavigateProfile`/`onNavigateAppSettings`) plus `onLogOut`/`onSwitchAccount`, and is threaded through `TargetNavHost` into each of these screens' composable calls.

**Study drawer group**: by explicit request ("group all these functions in the side menu under 'Study for your exam,' 'Study,' or a similar name"), Exams/Courses/Study Resources/Topics/Statistics/Languages are no longer six flat top-level drawer rows — they now sit under one collapsible **"Study"** header (`label_study_group`, `Icons.Filled.School`), with My Account/Settings/Help/Home unchanged below it. A private `DrawerGroupHeader` composable (`AppShell.kt`, styled to match `DrawerItem`'s own icon+label+padding shape so the header reads as one more drawer row, not a different visual language) renders the icon, label, and a chevron (`Icons.Filled.KeyboardArrowUp`/`KeyboardArrowDown`, reusing the existing `cd_collapse_x`/`cd_expand_x` content-description strings the same way `ExamDetailScreen`'s/`CourseListByTypeScreen`'s other collapsible-section chevrons already do) that toggles a local `studyExpanded` `remember { mutableStateOf(true) }`. The six grouped `DrawerItem`s gained an `indented: Boolean` param (`start = 24.dp` instead of `12.dp`) so they visually nest under the header. This **defaults to expanded**, unlike every other collapsible section in the app (which default closed, per the "reported as crowded" precedent) — deliberately, since these are the drawer's primary navigation destinations and the state isn't persisted (a fresh `AppShell` composition per screen, same as every other collapsible section), so defaulting closed would force re-expanding "Study" every single time the drawer opens on any screen. Confirmed on-device: the header shows the graduation-cap icon and "Study" label matching the other rows' visual weight, tapping it collapses/expands the six items with the chevron flipping correctly, and My Account/Settings/Help/Home stay in place below the group either way.

**My Account / Settings restructure**: by explicit request ("group the functions in the Settings section under a general heading... My Account → Profile, Log Out, Switch Account... Settings → App Settings"), the overflow menu's original three items (`User Settings` → Edit Profile, `App Settings` → theme toggle, `Account` → a second dropdown with Log Out/Switch Account) were replaced with two: **My Account** and **Settings**, each a real drawer-and-overflow shortcut to its own new full page rather than a dropdown/submenu. `ui/myaccount/MyAccountScreen.kt` (`MyAccountRoute`) is a `GroupedCard` of three tappable `ListItem` rows — Profile (relabeled from "User Settings", same underlying `UserEditRoute`/`UserEditScreen`, unchanged), Log Out, and Switch Account (`shellNavigation.onLogOut`/`onSwitchAccount`, same functions as before, just invoked from a page row instead of a dropdown item) — mirroring the `CourseListScreen`-style menu-row shape (`ListItemDefaults.colors(containerColor = Color.Transparent)` on every row, per the documented `GroupedCard`-hosting-`ListItem` gotcha). `ui/settings/SettingsHomeScreen.kt` (`SettingsHomeRoute`) is the same shape with one row, **App Settings**, leading to the pre-existing theme/icon/color/language screen (`SettingsRoute`/`SettingsScreen.kt`, entirely unchanged in content) — that screen's own title was changed from "Settings" to "App Settings" (`menu_app_settings`) since "Settings" now names the new parent page one level up; `settings_title` ("Settings") was repointed to the new page instead of being replaced, since the label is still accurate there. `onNavigateProfile`/`onNavigateAppSettings` are plain `navController.navigate(...)` pushes (not the `navigateToShellDestination` shell-lateral-nav helper), since `UserEditRoute`/`SettingsRoute` are now one level deeper than the drawer, matching the drill-down relationship `CourseDetailRoute` already has to `CourseListRoute` — `onNavigateMyAccount`/`onNavigateSettings` do use the shell helper, since `MyAccountRoute`/`SettingsHomeRoute` are the actual drawer/overflow-level destinations now. Confirmed on-device: the overflow menu shows exactly "My Account"/"Settings"; the drawer shows them in the confirmed order (`...Languages, My Account, Settings, Help, Home`) with correct active-destination highlighting; My Account's Profile row opens the real Edit Profile screen with the real Celikbas data intact; Settings' App Settings row opens the unchanged theme screen (Dark mode/App Icon/App Color/Language all intact, Powder Pink still selected); Switch Account still lists every other on-device account correctly excluding whichever is currently signed in; back navigation from every drilled-into screen returns to the correct parent page.

`ui/help/HelpScreen.kt` (`HelpRoute`), added by explicit request ("a 'how to use the app functions' named 'help' feature... on top of Home"), is a single scrollable `AppShell`-wrapped page — no navigation of its own, no ViewModel, just a static `Column` of nine `HelpSection(titleRes, bodyRes)` pairs (Getting Started, Exams, Courses, Study Resources, Topics, Languages, Quick Shortcuts, Backing Up a Course's Question Banks, Settings), each a short plain-language paragraph describing that feature as it actually works today — every title and body is its own `strings.xml`/`values-tr` pair (`help_*_title`/`help_*_body`), following the same "no hardcoded UI string literals" rule as everywhere else in the app. Confirmed on-device: Help sits directly above Home in the drawer with a divider between them; scrolled through all nine sections in both English and (after switching the language preference to verify) Turkish, with no layout overflow or cut-off text; switched the language preference back to English afterward.

**Log Out / Switch Account**: both now live as rows on `MyAccountScreen` (see My Account / Settings restructure above) rather than a nested overflow dropdown. Log Out (`TargetNavHost`'s `signOut` lambda) sets `AppPreferences.currentUserId = null` and navigates to `LoginRoute` with `popUpTo(0)`, clearing the whole back stack. Switch Account navigates to `SwitchAccountRoute` (`ui/switchaccount/SwitchAccountScreen.kt` + `SwitchAccountViewModel.kt`) — a back-arrow `Scaffold` (not `AppShell`, since it's a drill-down from `MyAccountScreen`, not a drawer destination) listing every other `User` row on the device (`UserDao.getAll()`, filtered to exclude the current session's id). Tapping one opens a password-confirmation `AlertDialog` — switching accounts re-verifies the target account's own password via `PasswordHasher.verify()` before setting `AppPreferences.currentUserId`, the same trust boundary as logging in fresh, rather than letting a signed-in session hop into another account without proof of that account's credentials. On success it navigates to `HomeRoute` with `popUpTo(0)`, same full-reset pattern as Log Out.

**Deleting an account**: by explicit request ("add to switch account feature a delete account function... pop up when user hold to the app listed cart"), each row in `SwitchAccountScreen`'s list is now `Modifier.combinedClickable(onClick = ..., onLongClick = ...)` — a plain tap still opens the existing password-confirmation switch dialog, a long-press opens a `DeleteAccountConfirmDialog` (`switch_account_delete_title`/`switch_account_delete_message`, explicitly naming that it removes "all of their exams, courses, languages, and study data" and can't be undone, since deleting a `User` cascades through every FK that points at it) that calls `SwitchAccountViewModel.deleteAccount(user)` → `UserDao.delete(user)` (a plain `@Delete`, the DAO's first). No manual list refresh is needed — `otherUsers` is already `Flow`-backed from `userDao.getAll()`, so the deleted row disappears immediately. A `HelpTooltip` (the 16th spot for this component, `help_tooltip_switch_account_delete` = "Press and hold an account to delete it.") sits in the top bar next to the back arrow, since a long-press gesture has no visible affordance of its own — this is a real gap in the otherwise-fully-scoped 15-spot `HelpTooltip` rollout documented in Interface redesign below, added after that rollout was already called complete. Deletion is **not** password-gated, unlike Switch Account's own login step — this matches every other delete confirmation in the app (courses, exams, topics, study resources all just ask "are you sure," no re-auth), and the target account is never the currently-signed-in one anyway, since `otherUsers` already excludes it. Confirmed on-device: the tooltip shows correct text; long-pressing a row opens the dialog with the correct account name substituted; Cancel leaves the account untouched; Delete removes it and the list updates immediately with no stale row. Used to clean up the two throwaway test accounts (`Switch`/`switchtest1`, `Switch2`/`switchtest2`) created earlier to test Switch Account's correct-password path — both deleted cleanly, confirming the feature doubles as real account cleanup, not just a demo. One on-device testing gotcha hit while verifying this (not an app bug): `adb shell input tap`'s near-instantaneous down/up was not registering as a click on this specific `AlertDialog`'s buttons at all (`Cancel`/`Delete`/even tapping the scrim to dismiss all did nothing, confirmed via `uiautomator dump` that the tap coordinates were pixel-exact on the buttons' real bounds) — switching to `adb shell input swipe X Y X Y 80` (a stationary swipe, i.e. a held tap of ~80ms) on the same coordinates worked immediately. Worth remembering for any future `AlertDialog` button-tap testing on this device.

`HomeScreen`'s welcome text bolds just the preferred name (`buildAnnotatedString` + `SpanStyle(fontWeight = FontWeight.Bold)` around `currentUser?.preferredName`, "Welcome to Target, " stays regular weight) — the one place in the app a single string mixes font weights inline rather than being a single `Text` call.

`ExamDetailScreen` and `SectionDetailScreen` are the deliberate exception — reached by drilling into a *specific* exam/section (not a drawer destination), they keep their own simple back-arrow `Scaffold` as before, not `AppShell`. `CourseListByTypeScreen`, `LanguageExamCoursesScreen`, `CourseDetailScreen`, `LanguageDetailScreen`, `CourseStudyResourceScreen`, `LanguageStudyResourceScreen`, `StudyResourceDetailScreen`, `TopicListScreen`, and `TopicDetailScreen` follow the same exception, since each is reached by drilling into a specific bucket/course/language/resource/topic from either `CourseListScreen`, `StudyResourceHomeScreen`, or `CourseDetailScreen`, not from the drawer directly.

Drawer/overflow navigation between the shell-level destinations uses `popUpTo<HomeRoute> { saveState = true }` + `launchSingleTop = true` + `restoreState = true` (a private `NavHostController.navigateToShellDestination()` extension in `TargetNavHost.kt`) — bouncing between Exams/Courses/Study Resources/Languages/My Account/Settings via the menu doesn't pile up back-stack entries the way plain repeated `navigate()` calls would.

Screen shape: `LoginScreen`/`RegisterScreen` → `HomeScreen` → `ExamListScreen` → `ExamDetailScreen` (edit/delete exam; Courses list hidden on Language Exams and on sectioned exams — see above for both; Sections shown when `hasSections`) → `SectionDetailScreen` (edit/delete section; course-subset picker, rows tap through to `CourseDetailScreen` same as `ExamDetailScreen`'s pool); `CourseListScreen`, `StudyResourceHomeScreen`, `LanguageListScreen`, `MyAccountScreen`, `SettingsHomeScreen` reachable from any shell screen's drawer/overflow menu, not just Home's. `MyAccountScreen` drills into `UserEditScreen` (Profile) and `SwitchAccountScreen`; `SettingsHomeScreen` drills into `SettingsScreen` (App Settings). `CourseListScreen` drills into `CourseListByTypeScreen` → `CourseDetailScreen` → `CourseStudyResourceScreen` and `LanguageExamCoursesScreen` → `LanguageDetailScreen` → `LanguageStudyResourceScreen` (edit/delete live on the Detail screens); `StudyResourceHomeScreen` drills into its own `CourseListByTypeScreen`/`LanguageExamCoursesScreen` instances (via `StudyResourceCourseListByTypeRoute`/`StudyResourceLanguageExamCoursesRoute`) that skip straight to `CourseStudyResourceScreen`/`LanguageStudyResourceScreen`, bypassing Detail — Study Resources is a shortcut to the study-resource list, not a second route into Detail. The drawer's `LanguageListScreen` also feeds into `LanguageDetailScreen`, same as the Courses flow. Add/Edit exam share one dialog, `ui/common/ExamFormDialog.kt`'s `AddOrEditExamDialog` — it's non-trivial (name field, exam-type picker, has-sections switch, conditional date picker(s)) so it's the one deliberate exception to this app's usual "duplicate small dialogs per screen" pattern (the other being `PreferredNameSelector`, above).

Forms with more than a couple of fields need `Modifier.imePadding()` on their scrollable content column, or the on-screen keyboard covers whatever's focused — `enableEdgeToEdge()` plus a manifest `windowSoftInputMode="adjustResize"` alone isn't sufficient in Compose. All of `LoginScreen`/`RegisterScreen`/`UserEditScreen` have it; apply it to any new multi-field form too.

### Interface redesign (in progress)

By explicit request ("everything feels boxy... let's make it a bit more rounded... an interactive experience"), the app is partway through a visual/interaction overhaul, planned in phases so nothing already working regresses. Two phases are done and pushed:

**Theme/shapes**: `ui/theme/Theme.kt`'s `TargetTheme` previously passed no `Shapes()` to `MaterialTheme(...)` at all, so every component used stock Material3 defaults (extraSmall 4/small 8/medium 12/large 16/extraLarge 28dp). A custom `AppShapes` (6/10/14/20/24dp respectively) is now passed — a "subtle/modern" bump, not a "bubbly" one, confirmed via clarifying question. `Button` is unaffected either way (Material3 renders it stadium-shaped regardless of the `Shapes()` scale) — the "boxy" feeling was actually coming from the complete absence of any `Card`/`Surface` grouping, not from button corners; color scheme (`pinnedColorScheme`) and typography were deliberately left out of scope.

**Drawer active-destination highlighting**: `AppShell.kt`'s `NavigationDrawerItem`s all hardcoded `selected = false` — the current destination was never actually highlighted. `AppShell` gained a `ShellDestination` enum (`HOME`/`EXAMS`/`COURSES`/`STUDY_RESOURCES`/`TOPICS`/`STATISTICS`/`LANGUAGES`/`HELP`/`OTHER`) and a `currentDestination` param (default `OTHER`, so old call sites still compile); all 10 `AppShell(...)` call sites now pass the correct value (`UserEditScreen`/`SettingsScreen` stay `OTHER` since neither has its own drawer row — they're reached via the overflow menu). Confirmed on-device across Home/Exams/Study Resources: the previously-active row's rounded pill highlight correctly moves to whichever destination is actually open.

**Shared components**, replacing patterns that used to be hand-copied per screen:
- `ui/common/SegmentedToggle.kt` — a generic `<T>` composable (`SegmentedToggleOption(value, label, dimmed)` list + `selected`/`onSelect`, plus `textStyle`/`maxLines`/`overflow`/`segmentContentPadding` to cover every existing call site's exact look) replacing 4 separate private "pill toggle" composables that had accumulated: `PreferredNameSelector.ToggleSegment` (First/Middle, with a `dimmed` state), `CourseDetailScreen.CategoryToggleSegment` (Unset/Quantitative/Verbal), `SettingsScreen.SettingsToggleSegment` (used twice — App Icon and Language), and `StatisticsScreen.PeriodSegment` (used for both the 4-segment Period toggle and the 2-segment Source toggle). This is an explicit, user-approved break from this app's previously-documented "each screen owns its own private copy" convention for this one visual pattern — don't reintroduce a fifth private copy for any future segmented toggle; use `SegmentedToggle`. Confirmed on-device: Settings' App Icon/Language toggles, the Preferred Name toggle (dimmed state included), and Course Detail's Category toggle all render and animate identically to before.
- `ui/common/AddFab.kt` — a real `Icons.Filled.Add` + `cd_add` content description, replacing all 7 screens' identical `FloatingActionButton(onClick=...) { Text("+") }` (a literal `+` glyph with no accessibility label at all — a real, if minor, pre-existing a11y gap fixed as a side effect). Confirmed on-device on the Exams screen.
- `ui/common/GroupedCard.kt` — `Card(shape = MaterialTheme.shapes.medium, colors = CardDefaults.cardColors(containerColor = MaterialTheme.colorScheme.surfaceContainer))` wrapping a `Column`, for grouping a whole logical section (not a per-row wrapper — wrapping all 72+ individual list rows across the app in their own card was considered and rejected as busier/heavier than "subtle/modern"). Piloted on `SettingsScreen`'s four preference sections (Dark Mode, App Icon, App Color, Language), each now a distinct rounded/tonal block instead of sitting directly on the screen background. Confirmed on-device.

**`CourseListByTypeScreen`/`LanguageExamCoursesScreen`** (the shared course-type-bucket drill-downs behind 5 routes across the Courses/Study Resources/Topics flows) now wrap each Quantitative/Verbal/Uncategorized group — and, for the Language Exam bucket, the flat French/German list — in a `GroupedCard`, confirmed on-device (a course row still correctly navigates to `CourseDetailScreen` from inside the card).

**Important gotcha, caught while verifying this on-device rather than by inspection**: `ListItem`'s default `containerColor` is opaque, so any `ListItem`-based row placed inside a `GroupedCard` fully **hides** the card's own tonal background wherever that row sits — only a plain `Row` (no `ListItem`, e.g. a group header) shows the intended tint. The on-device screenshot looked plausible at a glance (all one shade of near-black), and only direct pixel sampling (`System.Drawing.Bitmap.GetPixel()`, the same trick already used for `BannerColor` — see App Color Preference above) caught that a row's pixels were *identical* to the plain screen background, not just subtly different. Fixed by giving every `GroupedCard`-hosted `ListItem` row (`CourseRow`, `LanguageCourseRow`) an explicit `colors = ListItemDefaults.colors(containerColor = Color.Transparent)`. **Any future `GroupedCard` rollout that hosts `ListItem` rows needs this same fix** — it's easy to add a new card wrap, see a plausible-looking screenshot, and miss that the row backgrounds are silently opaque.

`GroupedCard` itself now applies `Modifier.animateContentSize()` to its inner `Column`, so **every** adopter's expand/collapse animates smoothly for free — no per-call-site work needed, and this retroactively benefited the `CourseListByTypeScreen`/`LanguageExamCoursesScreen` cards too. **`ExamDetailScreen`/`SectionDetailScreen`** now wrap their Courses/Sections lists (and, for `SectionDetailScreen`, its own Quantitative/Verbal/Uncategorized groups) in `GroupedCard` the same way, including the same `ListItemDefaults.colors(containerColor = Color.Transparent)` fix on every row (`CourseRow`, `SectionRow`, `AssignedCourseRow`). Confirmed on-device against the real YKS exam: the sectioned exam's Courses card correctly shows header-only (no chevron/list, per the existing `hasSections` gating), the Sections card expands smoothly to TYT/AYT, and TYT's own Quantitative group expands to its real Biology/Chemistry/Geometry/Mathematics/Physics courses with correct tint and working tap-through.

**`CourseListScreen`/`StudyResourceHomeScreen`/`TopicHomeScreen`** (the three exam-type-bucket-plus-inline-shortcut screens) now wrap each bucket (header row + its expanded inline course/language shortcuts) in one `GroupedCard`, same `ListItemDefaults.colors(containerColor = Color.Transparent)` fix applied to `ExamTypeBucketRow`/`CourseShortcutRow`/`LanguageShortcutRow` in all three files. Confirmed on-device: all buckets across all three screens render as distinct rounded cards, an expanded bucket's inline shortcuts sit inside one continuous card with correct tint, and shortcut-row taps still navigate straight to Course/Language Detail.

**GroupedCard rollout is now fully complete**, including `StudyResourceDetailScreen`'s Topics section — the one file treated with extra care given its hand-rolled drag-to-reorder list's documented history of subtle gesture bugs. Only the outer container (`TopicsSectionHeader` + the whole `ReorderableTopicsList` block, or the empty-state text) was wrapped in `GroupedCard`; nothing inside it was touched — `TopicRow`'s modifier chain, the `key(id){}` wrapping, `detectDragHandleGesture`, and the `graphicsLayer`/`zIndex` drag mechanics are all exactly as before. This deliberately leaves the same "ListItem hides the card tint" effect unfixed for this one screen's topic rows — a per-row color tweak was judged not worth the risk here, even though it's proven safe everywhere else; the header/empty-state area still shows the tint correctly. Confirmed on-device against the real "TYT Kimya Soru Bankası" (Miray Yayınları) Question Bank: the Topics header renders as a rounded card, a press-and-hold-then-drag (`adb shell input draganddrop`) correctly reordered two real topics, a plain row tap still opened `TopicProgressScreen` (confirming the drag-handle-vs-row-tap split survived), and the new order persisted through a full force-stop and relaunch — the test reorder was undone afterward to restore the real data's original order.

**`HelpTooltip`** (`ui/common/HelpTooltip.kt`) is a small `Icons.AutoMirrored.Outlined.HelpOutline` icon wrapped in a Material3 `TooltipBox`/`RichTooltip` — tap-to-show via a manually-driven `tooltipState.show()` (not the default long-press/hover trigger), since the user confirmed tap-to-show over a modal dialog. Wired into 15 explicitly-scoped, genuinely-non-obvious spots (not "everywhere"), each with its own `cd_help_<place>`/`help_tooltip_<place>` string pair in both locales:
1. `ExamFormDialog`'s "Has Sections" switch — decided at creation, can't change later.
2. `CourseDetailScreen`'s Category toggle — drives the Quantitative/Verbal/Uncategorized grouping seen in 4 other screens.
3. `StudyResourceFormDialog`'s Type field — only `QUESTION_BANK` + a course gets a Topics section.
4. `StudyResourceDetailScreen`'s Topics section header — Practice Session vs. Practice Exam distinction.
5–6. `TopicProgressScreen`'s aggregate/Net line and Remaining line.
7–8. `StatisticsScreen`'s Period and Source toggles (icon-only, placed above each — neither toggle had a visible label to attach to).
9–10. `CourseDetailScreen`'s Export and Import top-bar icons.
11. `SettingsScreen`'s App Color Preference heading.
12. `PreferredNameSelector`'s label.
13. `CourseDetailScreen`'s Exam Type dropdown.
14. `ExamDetailScreen`'s clickable "Language: X" line.
15. `CourseDetailScreen`'s Topics/Study Resources button pair (icon-only, placed above both buttons).

Confirmed on-device in all three distinct layout contexts the placements use: a plain screen (Settings' App Color tooltip — correct popover text, correct dismiss), a crowded `TopAppBar` (`CourseDetailScreen` — now 7 icons wide with Export/Import each getting their own tooltip, still fits with no overflow), and inside an `AlertDialog` (`ExamFormDialog`'s Has Sections tooltip — no crash, Cancel still works normally). One known, accepted cosmetic quirk: a top-bar-anchored tooltip has very little room above it, so it can slightly overlap the app bar instead of floating cleanly above the icon — still fully functional and readable, just not perfectly positioned; not worth a special-cased position provider for this first pass. The phone's wireless-debugging port changes across reconnects (already a known quirk — see Project conventions), which repeatedly interrupted this session's on-device verification; re-check `adb devices -l` each time before resuming.

**The interface redesign's planned scope is now complete**: theme `Shapes()`, drawer active-destination highlighting, the `SegmentedToggle`/`AddFab`/`GroupedCard` shared-component consolidation, a full `GroupedCard` rollout (including the drag-reorder-sensitive `StudyResourceDetailScreen`, handled in its own isolated commit), and `HelpTooltip` across the 15 spots above. Any further rounding/animation/tooltip work should be treated as a new, separately-scoped request rather than an extension of this plan.

## Explicitly out of scope right now (do not build unless asked)

- Managing `ExamType` directly (rename/delete) — called out by the user as a deferred "next step." (Course management now exists — see above.)
- Anything about lecture notes or pace estimation — all dropped in the reset, not part of the current model. (Question Bank topic/count tracking and logging actual solved/unsolved progress against those counts — see Topics/Practice Sessions — and Practice Exam containers/entries with their own results and optional topic breakdown — see Practice Exams — are both now in scope, but this doesn't extend to Lecture Notes resources unless asked. A combined, multiple-course practice exam — one "deneme" spanning every course in a single sitting, e.g. a full TYT mock — is explicitly a separate, bigger feature to plan after the single-course Practice Exam feature, not part of it.)
- Search, tags, local notifications/reminders, restore/import, sync/network access of any kind (accounts are now in scope, but they're local-only — no cloud sync). (A scoped one-way CSV *export* of a course's Question Bank data is now in scope — see below — but nothing reads a file back in, and no other export scope beyond that one course-level CSV.)

Don't scaffold data model fields or UI for any of the above speculatively — add them when explicitly requested.

## Hard constraint: this app must stay fully offline

- No `INTERNET` permission in the manifest. No network calls, no analytics SDKs, no crash reporting services that phone home. If a dependency wants network access, flag it before adding it.
- All data lives on-device only, in a local Room database.

## Tech stack

- **Language**: Kotlin
- **UI**: Jetpack Compose, Material 3 (plus `material-icons-extended` — added on top of the already-present `material-icons-core` specifically to get subject-appropriate course icons; still fully offline/bundled, no network, just a larger APK). Every "add" dialog uses `AlertDialog`; `ui/common/ExamFormDialog.kt` is the only shared cross-screen dialog so far (see above for why). Date input uses Material3's `DatePicker`/`DatePickerDialog` (`ui/common/DateFormatting.kt` has the shared display formatter) — first introduced for this model, not used in the discarded one.
- **Persistence**: Room (SQLite), currently **schema version 17** (`data/local/Migrations.kt`: `MIGRATION_1_2` added `users`/`userId` columns, `MIGRATION_2_3` added `User.preferredNameSource`, `MIGRATION_3_4` added `Course.icon`, `MIGRATION_4_5` added the `languages` table and `Exam.languageId`, `MIGRATION_5_6` added `Course.examTypeId` (best-effort backfilled per course from an exam it's already attached to, via a correlated subquery over `exam_courses`/`exams` — a course with no attachment, or an ambiguous multi-exam-type one, is left `NULL` and simply won't appear under any of the Courses screen's four buckets until re-added there, the same "orphaned rather than destroyed" precedent as the `userId` migration) and the `study_sources` table, `MIGRATION_6_7` added `Course.category`, `MIGRATION_7_8` added `StudySource.type` and `StudySource.publisher`, `MIGRATION_8_9` renamed `study_sources` to `study_resources` (`ALTER TABLE ... RENAME TO`, plus dropping and recreating its two indices under the new naming convention — an actual rename, not a drop/recreate, since there was real on-device data e.g. an "iTalki" resource under French to preserve) once "Study Sources" was renamed to "Study Resources" throughout the app, `MIGRATION_9_10` added the `topics` and `study_resource_topics` tables (see Topics above) — the first migration in the app to `CREATE TABLE` with a `NOT NULL ... REFERENCES` FK column and a `NOT NULL DEFAULT 0` numeric column, since every prior new-table migration used nullable FKs and no numeric columns existed yet, `MIGRATION_10_11` added `StudyResourceTopic.orderIndex` (`NOT NULL DEFAULT 0`) and backfilled it per-`studyResourceId` via a correlated `COUNT(*)`-based rank over `topics.name` (ties broken by the join row's own `id`) so the migration doesn't visually reorder any existing attached-topics list, `MIGRATION_11_12` added the `practice_logs` table (see Practice Sessions above) — same `CREATE TABLE` + `NOT NULL ... REFERENCES` shape as `MIGRATION_9_10`, no backfill needed since it's a brand-new log with no prior data, `MIGRATION_12_13` was the first (wrong-shape) pass at Practice Exams — added a `questionCount` column to `study_resources` and the since-abandoned `practice_exam_attempts`/`practice_exam_topic_results` tables — kept in the file unedited since it's exactly what already ran on real devices, `MIGRATION_13_14` corrects it (see Practice Exams above): recreates `study_resources` without `questionCount` via the standard SQLite drop-column-via-recreate pattern (the second table-recreate in this file, after `MIGRATION_8_9`'s rename), drops the wrong-shaped tables, and creates `practice_exam_entries`/`practice_exam_entry_topic_results` in their place — all the added-column migrations before `MIGRATION_12_13` are plain nullable `TEXT`, no index, no backfill possible since each is a brand-new concept with no relational data to infer it from, so every pre-existing row just comes back `NULL`/"Not set"), `MIGRATION_14_15` added `PracticeLog.questionCount` (see Practice Sessions above) — same plain nullable `ADD COLUMN`, no default, no backfill possible shape as `MIGRATION_12_13`'s `study_resources.questionCount`, `MIGRATION_15_16` added `Exam.level` (see the Publisher / Level split above) — same plain nullable `ADD COLUMN` shape again, `MIGRATION_16_17` added the `planner_events`/`planner_event_completions` tables (see Planner above) — the third `CREATE TABLE` migration in the file, same `NOT NULL ... REFERENCES` shape as `MIGRATION_9_10`/`MIGRATION_11_12`, no backfill needed since it's a brand-new feature with no prior data. Room 2.8.4 stores a nullable enum column like `CourseCategory?`/`StudyResourceType?` natively (as the enum's `.name`) — no `TypeConverter` needed, same as `User.preferredNameSource` needed none. The temporary `fallbackToDestructiveMigration` from the previous reset has been **removed** — real `Migration`s are back in force for all schema changes, including the ones that added accounts, since there's real on-device exam data to preserve.
- **Seeding**: prefer an idempotent "check count, insert if empty" DAO method called from `TargetApplication.onCreate()` over `RoomDatabase.Callback.onCreate()` — see the `ExamType` note above for why the callback approach silently failed.
- **Architecture**: MVVM — Composable screens + ViewModel (StateFlow) per screen area, `AndroidViewModel` + `SavedStateHandle.toRoute()` for nav-scoped ViewModels (no DI framework).
- **Navigation**: Jetpack Navigation Compose with type-safe (`kotlinx.serialization`) routes: `LoginRoute`, `RegisterRoute`, `HomeRoute`, `ExamListRoute`, `ExamDetailRoute(examId)`, `SectionDetailRoute(sectionId)`, `CourseListRoute`, `CourseListByTypeRoute(examTypeId)`, `LanguageExamCoursesRoute`, `CourseDetailRoute(courseId)`, `LanguageDetailRoute(languageId)`, `CourseStudyResourceRoute(courseId)`, `LanguageListRoute`, `LanguageStudyResourceRoute(languageId)`, `StudyResourceDetailRoute(studyResourceId)`, `StudyResourceHomeRoute`, `StudyResourceCourseListByTypeRoute(examTypeId)`, `StudyResourceLanguageExamCoursesRoute`, `TopicListRoute(courseId)`, `TopicDetailRoute(topicId)`, `TopicHomeRoute`, `TopicCourseListByTypeRoute(examTypeId)`, `TopicProgressRoute(studyResourceTopicId)`, `PracticeExamEntryDetailRoute(entryId)`, `StatisticsRoute`, `PlannerRoute`, `HelpRoute`, `UserEditRoute`, `SettingsRoute`, `MyAccountRoute`, `SettingsHomeRoute`, `SwitchAccountRoute`. `CourseListRoute`, `StudyResourceHomeRoute`, and `TopicHomeRoute` are three independent shell-level entry points, each with its own drill-down route(s) rendering the identical `CourseListByTypeScreen` (and, for the two with a Language Exam bucket, `LanguageExamCoursesScreen`) composables — the Courses pair (`CourseListByTypeRoute`/`LanguageExamCoursesRoute`) leads on into `CourseDetailRoute`/`LanguageDetailRoute` before the terminal `CourseStudyResourceRoute`/`LanguageStudyResourceRoute`; the Study Resources pair (`StudyResourceCourseListByTypeRoute`/`StudyResourceLanguageExamCoursesRoute`) skips straight to those terminal routes, since Study Resources is meant to be a shortcut directly to the study-resource list, not a second path through Detail; `TopicCourseListByTypeRoute` (Topics has no Language Exam counterpart, since Topics are Course-only) skips straight to the existing `TopicListRoute`, the same shortcut relationship. Start destination is conditional on `AppPreferences.currentUserId` (`HomeRoute` if set, else `LoginRoute`), computed once in `TargetNavHost` via `LocalContext.current`.

Target device is a single personal phone (Samsung, One UI 8.5, Android 16) — this app is not intended for distribution, so don't add back-compat workarounds for older Android versions unless asked. Check `app/build.gradle.kts` for the actual configured `minSdk`/`targetSdk` rather than assuming a value here.

## Project conventions

- Package id: `com.ridvan.target`
- Build/run: `./gradlew assembleDebug` to build, `./gradlew installDebug` to install to the connected device (or use Android Studio's Run button / VS Code equivalent). Deploy target is the developer's own phone over adb (USB or wireless debugging) — no emulator setup exists or is needed. The phone connects over wireless debugging, whose port changes across reconnects — re-check `adb devices -l` after any connection drop rather than assuming a cached serial still works.
- Git: commit after each feature is working and tested on-device, with a descriptive commit message, then push to `origin/main` (GitHub) — don't leave commits sitting local-only. Don't batch unrelated features into one commit.
- Editor: development happens primarily in VS Code with the Claude Code CLI; Android Studio is used occasionally just for SDK management / Gradle sync, not as the primary editor.
- On-device testing via adb input/screenshots is fine, but this is the developer's actively-used personal phone — check `dumpsys window | grep mCurrentFocus` before sending taps, and stop immediately (ask the developer to drive manually instead) if focus lands on an app other than Target, since that means real concurrent use is happening.

## Current status

Second-generation model (Exam/ExamType/Section/Course/Language) plus accounts (User/auth), the persistent navigation shell (`ui/shell/AppShell.kt`, present on Home/Exams/Courses/Study Resources/Languages/User/Settings, not just Home) with a real drawer banner image, Settings (theme toggle + White/Blue app icon preference), Courses restructured into per-exam-type buckets, per-course icons, Languages management (with the Language Exam type combobox), and a Study Resources feature (its own drawer entry plus reachable from Courses, with a Course/Language Detail page in between the list and the study-resource list) are all implemented and pushed. Confirmed building, migrating cleanly over real on-device exam data (including the `Course.examTypeId` backfill), and running end-to-end on-device (register, dark mode toggle, Exams/Courses/Study Resources/Languages via the drawer, User Settings/App Settings via the overflow menu, the preferred-name selector and old/new/confirm password change, the menu staying present across all shell screens, switching the launcher icon between white/blue, the exam-level course picker showing/multi-selecting existing courses now correctly scoped to the exam's own type, picking/saving a course icon and seeing it render consistently across the Courses list/exam course pool/section course lists, adding languages then picking one on a new Language Exam and seeing it show up in the exam list row and detail summary, the Courses screen's four exam-type buckets, drilling into a bucket to list its courses (no more inline edit/delete there), tapping "Language Exam Courses" landing on its own screen (not the Languages drawer screen), tapping a course or language opening its Detail page (exam type dropdown correctly excluding Language Exam, edit/delete working, Study Resources button reaching the correct study-resource list), the new Study Resources drawer entry showing the same four buckets, the Languages drawer shortcut now also routing into the Detail page for consistency, and adding a Study Resource under both a course and a language from every entry point). The Study Resources drawer flow initially aliased the Courses flow's drill-down routes and so incorrectly detoured through Course/Language Detail before reaching the study-resource list; fixed by giving Study Resources its own `StudyResourceCourseListByTypeRoute`/`StudyResourceLanguageExamCoursesRoute` that go straight to `CourseStudyResourceRoute`/`LanguageStudyResourceRoute` — confirmed on-device that tapping a course (Biology) or language (French) from Study Resources now lands directly on its study-resource list, while the same tap from the Courses flow still correctly goes to Detail first.

Log Out / Switch Account (`ui/switchaccount/`, plus `UserDao.getAll()`) is implemented, pushed, and now confirmed on-device end to end — see Navigation shell above for the mechanics (the Account submenu it originally launched from has since been folded into `MyAccountScreen`, see the My Account / Settings restructure below). Confirmed on real accounts: Log Out returns to Login with the session cleared; Switch Account lists other on-device accounts excluding whichever is current, rejects a wrong password with an inline "Incorrect password" error and leaves the dialog open, and accepts the correct password and lands on that account's Home with a full back-stack reset — verified in both directions between two freshly-registered throwaway test accounts (`switchtest1`/`switchtest2`) since the developer's own account passwords aren't known to the assistant.

`ExamDetailScreen`'s Courses and Sections lists are now collapsible (default closed, independent chevron toggles) — confirmed on-device on the YKS exam that both collapse/expand independently and default closed on screen entry.

Quantitative/Verbal course categorization (schema v7, `Course.category`) is implemented and pushed. Confirmed on-device: the 3-segment Unset/Quantitative/Verbal toggle in `CourseDetailScreen`'s edit dialog saves and shows on the summary; `CourseListByTypeScreen` groups University Entrance Exam courses into collapsible Quantitative/Verbal/Uncategorized sections correctly; the app migrates cleanly from v6 with existing courses landing in Uncategorized. Two bugs found and fixed while testing this: `ExamDetailScreen`'s Courses pool never wired course-row taps to navigation at all (now goes to `CourseDetailRoute`), and the Courses section was showing (uselessly) on Language Exams like Goethe A2, now hidden there entirely since Language Exams use their own single `languageId` field, not a course pool — which in turn led to making the "Language: X" summary line clickable (→ `LanguageDetailRoute`), the same tap-through-to-detail behavior courses already had.

Sectioned exams (e.g. YKS/TYT/AYT) had their exam-level Courses list hidden by request too — the "Courses" header/"+ Add" stay (still how a course joins the exam's pool), but the collapse chevron and course rows don't render, since a sectioned exam's courses are meant to be seen only under their section now. `SectionDetailScreen`'s course rows are tappable → `CourseDetailRoute`, and its assigned-courses list now groups into Quantitative/Verbal/Uncategorized the same as the other two course lists. Confirmed on-device on YKS: the exam page shows Courses as a bare "+ Add" with no list, TYT's course list groups Biology/Geometry/Mathematics under Quantitative and Turkish/History under Verbal, and tapping a course there reaches `CourseDetailScreen` → Study Resources.

Note on an earlier false alarm: while testing the category grouping, several courses' categories appeared to change unexpectedly between checks (Advanced Mathematics/Biology/Geometry showing Quantitative when they'd just been reset to Unset). This looked like a data-corruption bug from automated tap sequences hitting the wrong rows in a reordering list, but pulling the live `target.db` on-device and inspecting it directly (`adb exec-out run-as com.ridvan.target cat databases/target.db > local.db`, then `sqlite3`/Python) showed it was real concurrent usage — the developer was independently testing the same feature on the phone during gaps in the session (confirmed by them directly). Two lessons: (1) tapping by fixed coordinates into a list that reorders itself between actions is unreliable regardless — re-screenshot and re-locate the target after every change rather than chaining blind taps; (2) when on-device data looks wrong mid-session and the developer may have touched the phone in the meantime, pull and inspect the actual database before assuming a bug and "fixing" data that might be intentional.

Study Resource types (`StudyResourceType`), publisher, and a `StudyResourceDetailScreen` are implemented and pushed — see Study Resources above for the mechanics. Confirmed on-device: added a "Question Bank 2024" resource (type Question Bank, publisher "TestPub") under Geometry via the Study Resources drawer flow, saw the Subject field auto-populated and read-only in the Add dialog, saw the saved resource's type show as a list-row subtitle, opened its new Detail page showing Subject/Type/Publisher with working Edit (all three editable fields pre-filled correctly) and Delete in the top bar.

The whole "Study Sources" feature (including the entity, DAO, and `study_sources` table) was then renamed to "Study Resources" throughout, backend and frontend, by explicit request — see `MIGRATION_8_9` above. Confirmed on-device: migrates cleanly from v8 with existing resources intact (e.g. an "iTalki" resource under French still shows "Subject: French" / correct name post-migration), drawer item reads "Study Resources", and the Edit dialog correctly reads "Edit study resource". `PreferredNameSource` (`User.preferredNameSource`, an unrelated concept — where a preferred name is sourced from) was deliberately left untouched despite sharing the word "Source".

Topics (schema v10, `Topic`/`StudyResourceTopic`, the new Topics button on `CourseDetailScreen`, the Topics section on `StudyResourceDetailScreen` for Question Bank resources, and `ui/topiclist`/`ui/topicdetail`) are implemented and pushed — see Topics above for the mechanics. Confirmed on-device: the app migrates cleanly from v9 over real exam/course/study-resource data; on Geometry, added a "Triangles" topic both via the Course Detail → Topics screen and via a Question Bank's checklist-picker (confirming the same course-scoped pool backs both entry points); attached it to two different Question Bank resources (different publishers) with different test/question counts each; Course → Topics → Triangles correctly showed the summed total (8 tests · 200 questions) with a per-publisher breakdown, and tapping a breakdown row landed on that publisher's own `StudyResourceDetailScreen`; renaming and deleting the topic worked, with delete cascading its `StudyResourceTopic` rows cleanly (no crash, no orphaned data, confirmed via the remaining resource's Topics section going back to empty); a Language-based Question Bank resource (type `QUESTION_BANK`, `courseId == null`) correctly showed no Topics section at all, confirming the Course-only scope decision. The Test Count/Question Count fields are the app's first numeric inputs (`KeyboardType.Number` + digit-filtering + parse-at-save) — confirmed the numeric keyboard appears and non-digit input is rejected.

Turkish/English localization (see Localization above) is implemented and pushed — every screen, dialog, and error message externalized to `values/strings.xml`/`values-tr/strings.xml`, with a Language Preference toggle in Settings. Confirmed on-device end to end: switched to Turkish and walked through Home, the drawer, Courses (all four exam-type bucket titles, Quantitative/Verbal group headers), Course Detail, the Topics screen, the overflow menu and its Account submenu, and the Settings screen itself — all correctly translated; switched back to English and confirmed round-trip (no stuck-Turkish strings) and that Dark mode / App Icon preference and the current screen position survived the `recreate()` both directions. One bug caught and fixed before this was considered done: `home_welcome_prefix`'s trailing space (before the bolded preferred name) was being silently stripped by the XML parser, observed on-device as "hoş geldin,**Celikbas**!" with no space — fixed by wrapping the string value in escaped double quotes (see Localization above). A second, larger issue was caught and fixed the same way: the first implementation (a Compose-only `CompositionLocalProvider(LocalContext ...)` in `MainActivity`) left every dialog and dropdown menu's body/button text stuck in English — only text resolved outside the dialog (like a title passed in as a parameter) translated — traced to `Popup`/`Dialog` re-deriving `LocalContext` from the real Activity context rather than inheriting the Compose override; fixed by moving the locale override into `MainActivity.attachBaseContext()` plus an `Activity.recreate()` on toggle, confirmed via the exact same dialog (Add course) going from partially-English to fully Turkish.

By explicit follow-up request, "Courses" was renamed to "Dersler" throughout the Turkish translation (was "Kurslar" — see Localization above for why), and course names now translate for display when they match a fixed list of common academic subjects (`ui/common/CourseNameDisplay.kt`). Confirmed on-device: the drawer/menu word, the exam-type bucket titles ("Üniversiteye Giriş Sınavı Dersleri"), and the Quantitative/Verbal group contents all show "İleri Matematik"/"Biyoloji"/"Kimya"/"Geometri"/"Matematik"/"Tarih"/"Türkçe" for the real on-device courses; opening a translated course's Edit dialog correctly shows the raw English name ("Chemistry") in the editable field rather than the Turkish display text, confirming editing/saving can't silently corrupt the underlying name.

`StudyResourceDetailScreen`'s and `TopicDetailScreen`'s body `Column`s (and three checklist-picker `AlertDialog`s — `ExamDetailScreen`'s Add Course, `SectionDetailScreen`'s Add Courses, `StudyResourceDetailScreen`'s Add Topic) were missing `Modifier.verticalScroll(rememberScrollState())`, so once a list grew past one screenful its remainder was silently unreachable — no crash, no visual cue, just content you couldn't get to. Found on-device on a real Chemistry Question Bank with 13 attached topics (only 8 showing, no way to scroll to the rest); fixed by adding the scroll modifier, the same pattern `RegisterScreen`/`UserEditScreen` already use for their forms. Confirmed on-device: the same 13-topic list now scrolls to reveal the remaining topics ("Kimyasal Türler Arası Etkileşim", "Maddenin Halleri", "Mol Kavramı", "Periyodik Sistem", "Tepkime Türleri") that were previously stuck below the fold.

Manual reordering of a Question Bank's attached Topics (schema v11, `StudyResourceTopic.orderIndex`) is implemented and pushed — a question bank's topics no longer have to stay alphabetical, since a real book's table of contents rarely is. `StudyResourceTopicDao.getByStudyResourceId` now orders by `orderIndex ASC` instead of `topics.name ASC`; each `TopicRow` in `StudyResourceDetailScreen` got up/down `IconButton`s (`KeyboardArrowUp`/`KeyboardArrowDown`, disabled at the first/last position via `isFirst`/`isLast` computed from `forEachIndexed`, not plain `forEach`); `StudyResourceDetailViewModel.moveTopicUp`/`moveTopicDown` find the row's current index in `attachedTopics.value` and swap its `orderIndex` with the adjacent row's via two sequential DAO `update()` calls; `addTopic`/`addExistingTopics` append new attachments at `attachedTopics.value.size` rather than leaving them at the default 0, so newly-added topics land at the end instead of jumping to the front. Confirmed on-device: built and installed cleanly, the v10→v11 migration ran without a crash, the up/down arrows render with the correct row disabled at each end of a real 13-topic Chemistry Question Bank, and the list still scrolls correctly through all 13 rows (the `verticalScroll` fix above). Actually exercising several reorder taps surfaced a second "false alarm" of the same shape as the category-grouping one further up this file: the on-device topic order and several topics' test counts kept changing between screenshots taken seconds apart with no taps of mine in between; pulling and inspecting the live `target.db` showed a clean, non-duplicated `orderIndex` permutation (0..12, one write per row) rather than any sign of a write race, and the developer directly confirmed they were concurrently testing the same feature on the phone — so the changing data was real usage, not corruption. Reconfirms the same lesson: don't diagnose or "fix" on-device data that looks wrong mid-session without first checking whether the developer is on the phone.

The up/down `IconButton`s were then replaced with press-and-hold drag reordering, by explicit request ("its controls is a little bit hard... when user press and hold it can we moveable"). `TopicRow`'s trailing content is now a single `Icons.Filled.DragHandle` icon (`cd_drag_topic_handle` in both locales, replacing the now-deleted `cd_move_topic_up`/`cd_move_topic_down`); `ReorderableTopicsList` (private composable in `StudyResourceDetailScreen.kt`) holds a local `orderedIds: List<Long>` re-synced from the ViewModel's `attachedTopics` via `LaunchedEffect` whenever not actively dragging, plus a `draggingId`/`dragOffsetY` pair and an `itemHeightPx` map (populated via `Modifier.onSizeChanged` per row) used to decide when a dragged row has crossed the midpoint of its neighbor and should swap places in `orderedIds` (a live, in-memory reorder — the DB write happens once, in a batch, on drag end). `StudyResourceDetailViewModel.moveTopicUp`/`moveTopicDown` were deleted and replaced by a single `reorderTopics(orderedStudyResourceTopicIds: List<Long>)` that writes each row's new `orderIndex` from its position in that list — a `List<Long>` of arbitrary new order rather than a pairwise swap, since a real drag can relocate an item past several neighbors in one gesture, not just up/down by one.

Two non-obvious bugs were caught and fixed before this was considered working, both worth remembering for any future manually-built (non-`LazyColumn`) reorderable list in this app: (1) the per-row `TopicRow` calls inside `orderedIds.forEach { ... }` **must** be wrapped in `key(id) { ... }` — without it, Compose reuses each child slot by *position* rather than by topic identity, so the instant a drag causes a swap (the dragged row's index changes), the row's `pointerInput(id)`-keyed gesture coroutine gets cancelled mid-drag (a different `id` now occupies that slot), silently ending the drag without ever calling `onDragEnd`/persisting anything — this produces no crash and no visible error, it just quietly does nothing, so it's easy to mistake for "the gesture didn't fire" rather than "the gesture fired and then got killed." (2) the drag handle's gesture cannot be built on the stock `detectDragGesturesAfterLongPress` when it lives inside a `Modifier.verticalScroll` ancestor (which the whole `StudyResourceDetailScreen` body is, per the earlier scroll fix): that function doesn't consume pointer input during its long-press wait, so the ancestor `verticalScroll` — watching the same unconsumed movement — can win the gesture race and scroll the whole page instead of starting the drag (this reproduced reliably via `adb shell input draganddrop`, which we adopted for testing this specific gesture shape since a plain `input swipe` moves immediately and never gives a long-press detector time to arm). The fix was a small hand-written replacement, `detectDragHandleGesture` (private, same file): it consumes the initial `down` **immediately**, keeps consuming every pointer move during the long-press wait (`withTimeoutOrNull(viewConfiguration.longPressTimeoutMillis)`), and only then starts the real drag — so the ancestor scrollable never sees an unconsumed movement to react to, regardless of what the finger does before the hold completes. Confirmed on-device on the real 13-topic Chemistry Question Bank: a press-and-hold-then-drag on the handle icon cleanly relocates a topic without the page scrolling out from under the gesture, tapping anywhere else on the row still opens the "Edit counts" dialog exactly as before (the handle's gesture and the row's `clickable` are on separate composables, so they don't compete for taps), and the new order round-trips through process death (rebuild + reinstall + relaunch) correctly, including a persistence check via a WAL-inclusive `.db`/`.db-wal`/`.db-shm` pull — a raw `adb ... run-as ... cat databases/target.db` alone reads stale pre-write data whenever Room's WAL file hasn't been checkpointed back into the main file yet, which briefly looked like a "the reorder isn't saving" bug during this session before it was traced to the pull method rather than the app.

`StudyResourceListContent`'s row subtitle (the course/language-level study-resource list, both the Courses-flow and Study-Resources-flow entry points) was type-only, so a publisher entered on Add/Edit had no visible confirmation anywhere except the Detail screen one level deeper — reported directly ("user cant see the publisher... in device it is the main screen now"). Fixed by making `StudyResourceRow`'s subtitle `"$type · $publisher"` (new `sr_row_subtitle` format string, `%1$s · %2$s`, same separator convention as `counts_tests_questions`) when a publisher is set, falling back to the type alone when it's null — matching `StudyResourceDetailScreen`'s own "omit the line entirely when absent" convention rather than showing a literal "No publisher" (that fallback text is reserved for `TopicDetailScreen`'s per-publisher breakdown, where distinguishing "still no publisher" from "line not present" actually matters). Confirmed on-device: the three real Chemistry Question Bank rows now read "Soru Bankası · Miray Yayınları" / "Soru Bankası · eis Yayınları" / "Soru Bankası · ÜçDörtBeş".

A Duplicate action for Study Resources was added by explicit request ("study source copy paste funtion") — see the Study Resources bullet above for the mechanics. Confirmed on-device against the real 13-topic Chemistry Question Bank: tapping Duplicate created "TYT Kimya Soru Bankası (Kopya)" with the same publisher and all 13 topics carried over with their exact test/question counts and order intact (verified directly via a WAL-inclusive `.db` pull); the app navigated straight to the new copy's own Detail screen (which itself has a working Duplicate/Edit/Delete, confirming a copy can be copied further); deleting the test copy afterward correctly cascaded its copied `StudyResourceTopic` rows and returned to the course's resource list. The test copy was deleted after verification — this wasn't real study data to keep around.

App Color Preference (eight selectable drawer-banner-plus-accent colors) is implemented and pushed — see App Color Preference above for the mechanics. Confirmed on-device: the Settings swatch grid renders all eight colors with correct selection state; picking a non-Blue color immediately updates both the drawer banner image and the app-wide accent (buttons, FABs, active segmented-toggle backgrounds) to match; switching back to Blue fully restores the original Material You look with no regression. The developer was concurrently testing the same feature on the phone during part of this session (confirmed directly, the same "check before assuming a bug" lesson documented earlier in this file applies again) — an apparent color-selection inconsistency mid-verification turned out to be their own taps, not a bug.

Practice Sessions (schema v12, `PracticeLog`, the new `TopicProgressScreen` reached by tapping a Question Bank's topic row) are implemented and pushed — see Practice Sessions above for the mechanics. Confirmed on-device against the real 13-topic Chemistry Question Bank: the app migrates cleanly from v11; logging a session (3 tests, 20 solved, 5 unsolved, 1h30m) against "Kimya Bilimi" correctly showed the aggregate line "3 test · 20 doğru · 5 yanlış · Net 18,75 · Toplam 1sa 30dk" (net score and duration both correctly computed and Turkish-locale-formatted); the session row appeared with the right date/summary and reopened, exactly pre-filled, when tapped; Cancel on both the session-edit and target-counts-edit dialogs left data untouched; the detach confirmation correctly named the number of sessions that would cascade-delete; and a WAL-inclusive `.db` pull confirmed the exact row (schema, values, and `PRAGMA user_version` = 12) matched what the UI showed.

The session-count-cap feature (`TopicProgressScreen` clamping logged sessions to a topic's remaining target, plus a Remaining line) and the two progress-visibility follow-ups (topic rows showing remaining/target, `CourseStudyResourceScreen`'s course-level totals header) are both implemented and pushed — see their respective sections above for the mechanics; both confirmed on-device against the real Chemistry data with hand-verified numbers.

The Statistics screen (filterable by exam/course/topic, Daily/Weekly/Monthly/All-time period toggle, accuracy/pace derived metrics, a Canvas-drawn stacked bar trend chart, and a per-topic breakdown list) is implemented and pushed — see Statistics above for the mechanics and on-device confirmation. This completes the practice-session-logging arc the user originally asked for (log → see remaining/progress at every level → see it all aggregated with trends).

Practice Exams (schema v14, `PracticeExamEntry`/`PracticeExamEntryTopicResult`, inline on `StudyResourceDetailScreen`, plus the `Statistics` screen's new Practice Exams source and exam-type-grouped Exam filter) are implemented and pushed — see Practice Exams above for the full mechanics, including the mid-implementation redesign from a wrong single-question-count-per-resource model to the correct container/named-entry model, and the `MIGRATION_13_14` recreate that undid the wrong schema without touching real data. Confirmed on-device end to end against a real "Denemeler" Practice Exam container the developer built up themselves while testing (four named entries, "Kimya 1"–"Kimya 4"): the inline "Practice Exams" section on the resource detail page correctly showed "4 deneme · 11✓ / 1✗ · Doğruluk %91 · Toplam 0sa 16dk" with each entry's own correct/wrong/boş row (including a fully-blank untouched entry showing "0✓ / 0✗ / 7 boş"); opening "Kimya 2" showed "Soru sayısı: 7" and "4✓ / 1✗ / 2 boş · 0sa 8dk" with a working Topic Breakdown section; the Statistics screen's Practice Exams source showed the exact same aggregate numbers plus a pace metric and trend chart; and the Exam filter dropdown correctly grouped exams under "Üniversiteye Giriş Sınavı"/"Dil Sınavı" headers. The correct+wrong-vs-question-count cap (both at the entry level and, separately, at the per-topic-breakdown-row level) and the derived boş/blank count were both hand-verified against real numbers.

Two follow-up fixes/features landed right after, both implemented, pushed, and confirmed on-device — see Practice Exams above for the mechanics of both: the Topic Breakdown editor's correct/wrong fields now also cap against the entry's own remaining correct/wrong budget (not just that row's own question count), fixing a reported bug where a topic could log a wrong answer even though its entry had zero wrong answers overall; and the course/language Study Resources list gained a Type filter dropdown, with the course list's totals header now switching between the Question Bank target/progress numbers, a Practice Exam aggregate, or no header at all depending on the selected type.

The per-Section/per-Exam Progress CSV export (one row per course+topic, target counts plus cumulative Practice Session solved/correct/wrong, meant as AI weekly-planning input) is implemented, pushed, and confirmed on-device — see Progress CSV export above for the mechanics and the real "TYT - Progress.csv" export it was verified against.

The Practice Session per-session question count (schema v15, `PracticeLog.questionCount`, the new Question Count field in `LogSessionDialog`, and the blank/"boş" tracking it enables) is implemented, pushed, and confirmed on-device — see the Practice Sessions section above (the paragraph following the App Color Preference on-device confirmation) for the full mechanics and the real "Sayı Kümeleri" session it was fixed and verified against, including the propagated fix to `StudyResourceDetailScreen`'s topic rows, `CourseStudyResourceScreen`'s totals header, and the Progress CSV export.

The interface redesign is fully implemented, pushed, and confirmed on-device: theme `Shapes()`, drawer active-destination highlighting, the `SegmentedToggle`/`AddFab`/`GroupedCard` shared-component consolidation, a full `GroupedCard` rollout across every collapsible section/bucket in the app (including the drag-to-reorder-sensitive `StudyResourceDetailScreen`, handled in its own isolated commit), and `HelpTooltip` wired into 15 explicitly-scoped spots — with a real bug fixed along the way (ListItem's opaque containerColor hiding the card background, caught only by direct pixel sampling, not by eyeballing the screenshot). See Interface redesign above for the full mechanics, the exact 15 placements, and the one known cosmetic quirk (a top-bar-anchored tooltip can slightly overlap the app bar).

A real, pre-existing crash in the Statistics screen (predating the redesign, traced via `adb logcat -b crash` back to 09-14) was found and fixed — see Statistics above for the root cause (`progress_aggregate`/`session_row_summary` missing their newer blank-count argument at two call sites) and the fix.

By explicit follow-up request ("the rounded highlight is too big... make text a little bit smaller and make them closer to each other"), the drawer's `NavigationDrawerItem`s and their per-item `HorizontalDivider`s were replaced with a compact custom `DrawerItem` row and no dividers between destinations — see Navigation shell above. Confirmed on-device: the selected-destination pill now hugs the icon/label instead of standing as a tall full-bleed bar, and switching destinations still moves the highlight correctly.

The Home dashboard (live clock + weekday/date card, "Upcoming" exam/section countdown card) and the Exams list's per-card day countdowns (including sectioned exams showing their real section names instead of "Sectioned") are implemented, pushed, and confirmed on-device — see Home dashboard above for the full mechanics and the real Goethe A2/YKS numbers it was verified against.

The My Account / Settings restructure (new `MyAccountScreen`/`SettingsHomeScreen` drawer-and-overflow destinations, "User Settings" relabeled "Profile", the old "Settings" screen relabeled "App Settings" one level deeper) is implemented, pushed, and confirmed on-device end to end — see the My Account / Settings restructure paragraph above for the full mechanics. Confirmed on the real Celikbas account: the overflow menu and drawer both show "My Account"/"Settings" in the agreed order; My Account's Profile/Log Out/Switch Account rows all work correctly (Profile shows the real pre-filled data, Switch Account lists every other on-device account); Settings' App Settings row opens the unchanged theme/icon/color/language screen with all preferences intact (Powder Pink accent, English, White icon); back navigation from every drilled-into screen returns to the correct parent. Two throwaway test accounts (`switchtest1`/`Switch`, `switchtest2`/`Switch2`) were created during this session's testing to exercise Switch Account's correct-password path without needing the developer's real password; both were deleted afterward using the account-deletion feature described next, once it existed.

Press-and-hold account deletion (long-press a row in Switch Account, `UserDao.delete`) is implemented, pushed, and confirmed on-device — see the Deleting an account paragraph above for the full mechanics. Confirmed: the new `HelpTooltip` shows correct text, long-press opens the confirmation dialog with the right account name, Cancel leaves data untouched, and Delete removes the account with the list updating immediately (no manual refresh, no stale row). Used in practice to delete the two leftover throwaway test accounts noted above, leaving only the developer's own real accounts on-device.

Tappable Home "Upcoming" rows (exam events go to `ExamDetailScreen`, section events go straight to that section's own `SectionDetailScreen`), then restyled as underlined links whose color animates to the app's accent on press, are implemented, pushed, and confirmed on-device — see Home dashboard above for the full mechanics and the real Goethe A2/YKS TYT confirmation.

The Language Exam Publisher / Level split (schema v16, `Exam.level`, the relabeled "Publisher" field + new "Level" field in `AddOrEditExamDialog`, the new `examDisplayLabel()` helper, and Home's own dash-joined "Goethe - A2 - German" format) is implemented, pushed, and confirmed on-device — see the Publisher / Level split paragraph above for the full mechanics and the real Goethe A2 exam it was verified against (migrated cleanly from v15, then re-split into Publisher "Goethe" + Level "A2" through the edit dialog, correctly propagating to the summary, screen title, Exams list row, and Home's Upcoming card).

The Study drawer group (Exams/Courses/Study Resources/Topics/Statistics/Languages collapsed under one "Study" header with a graduation-cap icon and chevron, defaulting expanded) is implemented, pushed, and confirmed on-device — see the Study drawer group paragraph above for the full mechanics. Confirmed: the header renders with the same icon/label/padding weight as the other drawer rows, tapping it collapses the six grouped items (chevron flips to point down) and re-expands them correctly (chevron flips back up), and My Account/Settings/Help/Home stay unaffected in both states.

The Planner (schema v17, `PlannerEvent`/`PlannerEventCompletion`, `PlannerHomeScreen`'s Week/Month/Year agenda merging planner events with exam/section dates, and core library desugaring newly enabled for `java.time`) is implemented, pushed, and has real developer-created data on-device ("Football match with friends," "Tarkovsky Movies Event") predating this session — see the Planner section above for the full mechanics. The Home preview card surfacing it (a third "Planner" `GroupedCard` below Clock/Upcoming, up to 6 upcoming events over the next 13 days with an inline completion checkbox and a link into the full Planner) is implemented, pushed, and confirmed on-device this session against that same real data: the card showed "Tarkovsky Movies Event · Today," toggling its checkbox updated completion state and round-tripped correctly, both the checkbox-adjacent title and the header's "Open Planner" link opened the full Planner (Week view, correct Sep 14–20 range, the real "Football match with friends" event visible with its own completion state intact), and back navigation returned cleanly to Home.

Three explicit follow-ups landed right after, all implemented, pushed, and confirmed on-device — see the Planner section's "Dates and time-of-day" and `PlannerEvent` bullets above for the full mechanics: (1) both the Upcoming and Planner cards now show the actual calendar date under their relative "X days left" label; (2) a Planner row additionally shows the event's time range when it's Today or Tomorrow; (3) `PlannerEventFormDialog`'s free-typed, could-be-zero "Duration (minutes)" field was replaced with a required End Time picker alongside Start Time. Confirmed together against the real "Tarkovsky Movies Event," which had in fact been sitting on-device with a genuine 0-minute duration (displaying as "17:00 – 17:00") — the new dialog surfaced this plainly as an editable End Time of 17:00, changing it to 18:00 and saving correctly updated the row to "17:00 – 18:00" on both the full Planner and Home's preview card, and "Mum's Birthday" (4 days out, all-day) correctly showed only its date with no time line, confirming the Today/Tomorrow-only scoping held.
